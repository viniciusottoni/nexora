Settings toggle. The thumb springs across; the track fills with brand energy when on.

```jsx
<Switch checked={notifs} onChange={setNotifs} label="Lembretes de streak" />
```

`onChange` receives the next boolean. Optional `label` renders an inline, tappable row.
