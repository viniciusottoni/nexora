# EPIC-005 — US-236 a US-240 Implementation Plan

> **For agentic workers:** Execute task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Each task lists files, exact signatures/fields and test scenarios (Given/When/Then, sourced from the US's own CA/QA sections). Follow existing repo conventions found during research (see "Convenções confirmadas" per task) — do not invent new architectural patterns when an existing one already fits.

**Goal:** Implement US-236 (taxonomia biomecânica + relações + mídia GIF), US-237 (split map), US-238 (rotação do dia), US-239 (recuperação muscular) e US-240 (blueprint do dia) do EPIC-005, ligando tudo ao pipeline de geração de quest já existente (`WorkoutGeneratorService`), sem quebrar os testes existentes.

**Architecture:** Reaproveita ao máximo o que já existe (DRY/YAGNI — CLAUDE.md). Cada US vira um serviço de domínio *puro* (static, sem I/O) quando a lógica é determinística (rotação, recuperação, blueprint), orquestrado por um serviço de aplicação/infra que busca dados via repositório. Novidades de schema são mínimas e aditivas (novas colunas nullable, duas tabelas novas para o split map, uma tabela nova para recuperação). Nenhuma tabela é criada quando o dado já existe em outro lugar (ver seção "Desvios conscientes do texto literal da spec").

**Tech Stack:** ASP.NET Core (.NET 10) + EF Core (Postgres/Npgsql) + MediatR/CQRS + FluentValidation, xUnit + FluentAssertions + Moq (backend); Flutter + Riverpod + go_router (frontend); AWSSDK.S3 para Cloudflare R2 (novo).

---

## 0. Descobertas de pesquisa que mudam o plano

Pesquisa no código (não na doc) mostrou que parte do que as USes pedem **já existe**, implementado durante o US-143/US-144 (`ImportExercisesCommandHandler`), só que sob nomes/estruturas diferentes dos citados nas USes novas. Ignorar isso e implementar ao pé da letra criaria duplicação (proibido pelo CLAUDE.md: "don't add abstractions beyond what the task requires").

**Decisão do usuário (confirmada antes da execução):** criar as tabelas novas literalmente, e apertar `CanBeApproved()` para exigir GIF. Abaixo, como isso é feito sem virar duplicação sem sentido:

| Spec pede | Já existe no código | Decisão |
|---|---|---|
| `ExerciseTaxonomy` (tabela nova 1:1) | Campos de taxonomia já inline em `ExerciseCatalog` (`MovementFamily`, `MovementPattern`, `Mechanic`, `ForceType`, `PlaneOfMotion`, `Laterality`, `BodyPosition`, `BenchAngle`, `EquipmentCategory`, `LoadType`, `PrimaryRegion`, `IsCompound`, `IsUnilateral`, `IsAssisted`, `IsWeighted`, `TaxonomySignals`, `Confidence`), já populados por `ImportExercisesCommandHandler.BuildCatalogSnapshot`. | **Extrair de verdade** para uma tabela `ExerciseTaxonomy` (1:1, FK `ExerciseCatalogId`), com migração movendo os dados das colunas atuais. Para não estourar o raio de mudança em todo mundo que lê `exercise.MovementPattern` etc. (scoring engine, selection engine, mapper, US-238/239/240), `ExerciseCatalog` mantém propriedades somente-leitura com o mesmo nome que **delegam** para `Taxonomy` (`public string MovementPattern => Taxonomy?.MovementPattern ?? "";`). `ExerciseCatalogSnapshot` (usado pelo import) não muda de forma — `Apply()` por dentro passa a criar/atualizar o `Taxonomy` filho em vez de campos locais. Ver Task 1.0. |
| `ExerciseRelationship` (tabela nova) | `ExerciseCatalogRelation` já tem exatamente os campos pedidos (`RelatedProviderExerciseId/RelatedName/RelationKind/Types/Score/Confidence/Reasons`), populada em todo import via `BuildRelations`, só falta leitura + `DatasetVersion`. | **Renomear a entidade/tabela** `ExerciseCatalogRelation` → `ExerciseRelationship` (rename de classe + `RenameTable` na migration), em vez de duplicar os mesmos dados em duas tabelas paralelas — isso seria a forma errada de "criar a tabela nova" (duplicação sem propósito, contra CLAUDE.md). O resultado é literalmente uma entidade chamada `ExerciseRelationship`, com o dado real migrado, mais a coluna `DatasetVersion` nova. Ver Task 1.2. |
| GIF enviado para R2 | `FindMediaFile` acha o arquivo local e grava só uma *chave relativa* (`{batchKey}/{arquivo}`) em `GifUrl` — nenhum upload real acontece. `AWSSDK.S3` está no `.csproj` mas não é usado em lugar nenhum. | **Implementar upload real.** Nova interface `IMediaStorageService` + implementação R2. |
| `WorkoutSession` (entidade citada nas USes 238/239) | Não existe. O equivalente real é `Quest` (com `TrainingType`/`ProgramId`/`Status`/`CompletedAtUtc`) + `QuestExercise` (com `ExerciseCatalogProviderId`/`CompletedAtUtc`). | **Não criar `WorkoutSession`.** Estender `Quest` com os campos que a US-238 pede (`ResolvedProgramKey/DayKey/DayIndex/SplitMapVersion`) e derivar "sessão concluída" de `Quest.Status == "completed" && TrainingType == "program"`. |
| Enum `MuscleGroup` / `MovementPattern` (US-036/145) | Não existem como enum — são `List<string>`/`string` livres. Não há classe de constantes. | Criar `MuscleGroups`/`MovementPatterns` como *static class* de constantes + `HashSet` de validação, no mesmo padrão de `TrainingProgramKeys` (já existente) — não introduzir um paradigma de enum novo e inconsistente. |
| RN-006 (US-236): "nenhum exercício aprovado sem GIF 360" | `ExerciseCatalog.CanBeApproved()` aceita Video **ou** Image **ou** Gif. | Alterar para exigir especificamente `GifUrl`. É uma mudança de comportamento deliberada e testável (CA-003 da US-236). Rodar a suíte inteira depois e corrigir quem quebrar. |
| RN-003 (US-236): divergência de taxonomia não sobrescreve, sinaliza revisão | Hoje há um único pipeline de import (a doc assumia dois pipelines concorrentes: API viva x dataset enriquecido). Com um único pipeline, não há dois escritores concorrentes divergindo. | **Não implementar detecção de divergência.** Documentar a decisão: a race condition que a regra previne não existe nesta arquitetura. |
| Endpoint de import do dataset enriquecido | `POST /api/admin/exercises/import` (`ImportExercisesCommand`) já cobre o parsing completo do arquivo enriquecido (taxonomy + relations). | **Não duplicar o handler.** Estender `ImportExercisesCommand`/`Request` com `DatasetVersion`/`SourceFile` opcionais, sem endpoint novo. |

---

## 1. Estrutura de arquivos (visão geral)

**Novo (backend):**
```
backend/src/Awaken.Domain/Entities/Exercises/ExerciseTaxonomy.cs
backend/src/Awaken.Infrastructure/Persistence/Configurations/ExerciseTaxonomyConfiguration.cs
backend/src/Awaken.Domain/Entities/Exercises/ExerciseRelationship.cs  (substitui ExerciseCatalogRelation.cs, deletado)
backend/src/Awaken.Infrastructure/Persistence/Configurations/ExerciseRelationshipConfiguration.cs (substitui ExerciseCatalogRelationConfiguration.cs, deletado)
backend/src/Awaken.Application/Common/Interfaces/IMediaStorageService.cs
backend/src/Awaken.Infrastructure/Services/CloudflareR2MediaStorageService.cs
backend/src/Awaken.Domain/Entities/Training/TrainingProgramSplit.cs
backend/src/Awaken.Domain/Entities/Training/TrainingSplitDay.cs
backend/src/Awaken.Domain/Entities/Training/MuscleGroups.cs
backend/src/Awaken.Domain/Entities/Training/MovementPatterns.cs
backend/src/Awaken.Domain/Repositories/ITrainingProgramSplitRepository.cs
backend/src/Awaken.Infrastructure/Persistence/Repositories/TrainingProgramSplitRepository.cs
backend/src/Awaken.Infrastructure/Persistence/Configurations/TrainingProgramSplitConfiguration.cs
backend/src/Awaken.Infrastructure/Persistence/Configurations/TrainingSplitDayConfiguration.cs
backend/src/Awaken.Domain/Services/Training/DailyProgramDayResolver.cs
backend/src/Awaken.Domain/Entities/Training/MuscleRecoveryState.cs
backend/src/Awaken.Domain/Repositories/IMuscleRecoveryStateRepository.cs
backend/src/Awaken.Infrastructure/Persistence/Repositories/MuscleRecoveryStateRepository.cs
backend/src/Awaken.Infrastructure/Persistence/Configurations/MuscleRecoveryStateConfiguration.cs
backend/src/Awaken.Domain/Services/Training/RecoveryPlanner.cs
backend/src/Awaken.Domain/Services/Training/DailyWorkoutBlueprintBuilder.cs  (ou em Application, ver Task 5.1)
backend/src/Awaken.Application/TrainingPrograms/Queries/GetTrainingProgramSplit/*.cs
backend/src/Awaken.Application/Quests/Queries/GetResolvedProgramDay/*.cs
backend/src/Awaken.Application/Quests/Queries/GetRecoveryPlan/*.cs
backend/src/Awaken.Application/Exercises/Queries/GetExerciseRelationships/*.cs
backend/src/Awaken.Contracts/TrainingPrograms/TrainingProgramSplitResponse.cs
backend/src/Awaken.Contracts/Quests/ResolvedProgramDayResponse.cs
backend/src/Awaken.Contracts/Quests/RecoveryPlanResponse.cs
backend/src/Awaken.Contracts/Exercises/ExerciseRelationshipResponse.cs
backend/src/Awaken.Api/Controllers/V1/ExercisesController.cs (novo endpoint na existente, ou criar se não houver pública)
backend/src/Awaken.Infrastructure/Persistence/Migrations/20260706*_AddTrainingProgramSplit.cs (+ Seed)
backend/src/Awaken.Infrastructure/Persistence/Migrations/20260706*_AddMuscleRecoveryState.cs
backend/src/Awaken.Infrastructure/Persistence/Migrations/20260706*_AddDatasetVersionAndResolvedDayFields.cs
backend/tests/Awaken.UnitTests/Training/*.cs  (novos)
backend/tests/Awaken.UnitTests/Quests/DailyProgramDayResolverTests.cs (ou Training/)
```

**Modificado (backend):**
```
backend/src/Awaken.Domain/Entities/Exercises/ExerciseCatalog.cs        (CanBeApproved; remove campos de taxonomia, ganha nav Taxonomy + getters delegados)
backend/src/Awaken.Domain/Entities/Exercises/ExerciseRawImport.cs      (+DatasetVersion, +SourceFile)
backend/src/Awaken.Application/Exercises/Commands/ImportExercises/ImportExercisesCommand.cs
backend/src/Awaken.Application/Exercises/Commands/ImportExercises/ImportExercisesCommandHandler.cs (upload R2)
backend/src/Awaken.Contracts/Exercises/ImportExercisesRequest.cs / Response.cs
backend/src/Awaken.Domain/Entities/Quests/Quest.cs                     (+Resolved*, +DailyWorkoutBlueprintJson)
backend/src/Awaken.Domain/Repositories/IQuestRepository.cs             (+GetLastCompletedByUserAndProgramAsync)
backend/src/Awaken.Infrastructure/Persistence/Repositories/QuestRepository.cs
backend/src/Awaken.Infrastructure/Services/WorkoutGeneratorService.cs  (integra blueprint)
backend/src/Awaken.Application/Quests/Commands/CompleteQuest/CompleteQuestCommandHandler.cs (atualiza MuscleRecoveryState)
backend/src/Awaken.Infrastructure/Persistence/AwakenDbContext.cs       (+DbSets)
backend/src/Awaken.Infrastructure/DependencyInjection.cs              (+registros)
backend/src/Awaken.Api/appsettings.json                                (Cloudflare já tem placeholders, ok)
```

**Novo/Modificado (Flutter):**
```
apps/mobile/lib/features/quests/domain/entities/quest_preview.dart          (+gifUrl)
apps/mobile/lib/features/quests/domain/entities/quest_execution.dart        (+gifUrl)
apps/mobile/lib/features/quests/data/dtos/quest_execution_response_dto.dart (+gifUrl)
apps/mobile/lib/features/quests/data/dtos/quest_response_dto.dart           (+gifUrl)
apps/mobile/lib/features/quests/presentation/widgets/pre_quest_exercise_card.dart (exibe gif)
apps/mobile/lib/features/quests/presentation/pages/exercise_execution_page.dart  (exibe gif + secao alternativas)
apps/mobile/lib/features/quests/presentation/widgets/exercise_alternatives_sheet.dart (novo)
apps/mobile/lib/features/quests/data/datasources/exercise_relationships_data_source.dart (novo)
apps/mobile/lib/features/quests/domain/entities/exercise_relationship.dart (novo)
apps/mobile/lib/features/training_programs/domain/entities/training_program_split.dart (novo)
apps/mobile/lib/features/training_programs/data/datasources/training_programs_remote_data_source.dart (+getSplit)
apps/mobile/lib/features/training_programs/presentation/pages/training_programs_page.dart (exibe split real)
apps/mobile/lib/l10n/app_{pt,en,es,fr}.arb (+chaves novas)
apps/mobile/test/features/quests/... (+testes novos)
apps/mobile/test/features/training_programs/... (+testes novos)
```

---

## 2. Ondas de execução (para dispatch em subagentes paralelos)

```
Onda 1 (paralelo, arquivos disjuntos):
  Agente A → Task Group 1 (US-236: R2 upload, dataset version, endpoint de relações, CanBeApproved, Flutter GIF+alternativas)
  Agente B → Task Group 2 (US-237: split map completo, backend + Flutter)

Onda 2 (paralelo, depende da Onda 1 apenas por leitura de tipos, arquivos disjuntos):
  Agente C → Task Group 3 (US-238: resolução do dia — depende do serviço de consulta do split da Task 2.2)
  Agente D → Task Group 4 (US-239: recuperação muscular — depende de MovementFamily/relações da Task 1, não depende de C)

Onda 3 (sequencial, integra tudo):
  Agente E → Task Group 5 (US-240: blueprint orquestrador, integração no WorkoutGeneratorService, Flutter labels/aviso, correção de testes quebrados em toda a suíte)
```

Cada agente deve rodar `dotnet build` + `dotnet test` (backend) e `flutter analyze` + `flutter test` (frontend, quando aplicável) antes de reportar a task como concluída, e corrigir qualquer teste quebrado por sua mudança.

---

## Task Group 1 — US-236: gaps restantes (Agente A, Onda 1)

**Convenções confirmadas:** handler em `Awaken.Application/Exercises/Commands/ImportExercises/`; controller admin em `AdminExercisesController.cs` (`[Authorize(Policy = "Admin")]`); DI em `Awaken.Infrastructure/DependencyInjection.cs`; testes em `backend/tests/Awaken.UnitTests/Quests/` e (para Exercises) provavelmente um novo diretório `backend/tests/Awaken.UnitTests/Exercises/` — confirme se já existe antes de criar.

**Regra geral desta task group (pedido explícito do usuário): não deixar duplicação.** Onde uma tabela nova é criada, o dado sai de onde estava antes — não fica em duas tabelas ao mesmo tempo. As propriedades "delegadas" descritas abaixo (ex.: `ExerciseCatalog.MovementPattern`) não são armazenamento duplicado: são getters calculados, sem coluna própria, só para não obrigar todo consumidor existente (scoring engine, selection engine, mapper) a mudar de `exercise.MovementPattern` para `exercise.Taxonomy.MovementPattern` num único diff gigante.

### 1.0 — Extrair `ExerciseTaxonomy` (tabela nova 1:1, sem duplicar dado)

**Files:**
- Create: `backend/src/Awaken.Domain/Entities/Exercises/ExerciseTaxonomy.cs`
- Create: `backend/src/Awaken.Infrastructure/Persistence/Configurations/ExerciseTaxonomyConfiguration.cs`
- Modify: `backend/src/Awaken.Domain/Entities/Exercises/ExerciseCatalog.cs` — remover as 16 propriedades de taxonomia como campos próprios; adicionar `public ExerciseTaxonomy? Taxonomy { get; private set; }` e getters delegados (só leitura, sem coluna).
- Modify: `backend/src/Awaken.Infrastructure/Persistence/Configurations/ExerciseCatalogConfiguration.cs` — remover o mapeamento das 16 colunas de taxonomia; adicionar `builder.HasOne(e => e.Taxonomy).WithOne().HasForeignKey<ExerciseTaxonomy>(t => t.ExerciseCatalogId).OnDelete(DeleteBehavior.Cascade);`.
- Modify: `backend/src/Awaken.Infrastructure/Persistence/AwakenDbContext.cs` — `DbSet<ExerciseTaxonomy> ExerciseTaxonomies`.
- Migration: `backend/src/Awaken.Infrastructure/Persistence/Migrations/20260706HHMMSS_ExtractExerciseTaxonomy.cs` — **schema + backfill em uma migration** (não é seed de referência, é migração de dado real de produção-dev, então segue o fluxo padrão do EF, só que com um passo manual de `Sql(...)` para copiar os dados antes de derrubar as colunas):
  1. `migrationBuilder.CreateTable("exercise_taxonomies", ...)` com as 16 colunas + `Id`/`ExerciseCatalogId`/`CreatedAtUtc`/`UpdatedAtUtc`, FK única em `ExerciseCatalogId`.
  2. `migrationBuilder.Sql(@"INSERT INTO exercise_taxonomies (""Id"", ""ExerciseCatalogId"", ""MovementFamily"", ""MovementPattern"", ""Mechanic"", ""ForceType"", ""PlaneOfMotion"", ""Laterality"", ""BodyPosition"", ""BenchAngle"", ""EquipmentCategory"", ""LoadType"", ""PrimaryRegion"", ""IsCompound"", ""IsUnilateral"", ""IsAssisted"", ""IsWeighted"", ""Signals"", ""Confidence"", ""CreatedAtUtc"", ""UpdatedAtUtc"") SELECT gen_random_uuid(), ""Id"", ""MovementFamily"", ""MovementPattern"", ""Mechanic"", ""ForceType"", ""PlaneOfMotion"", ""Laterality"", ""BodyPosition"", ""BenchAngle"", ""EquipmentCategory"", ""LoadType"", ""PrimaryRegion"", ""IsCompound"", ""IsUnilateral"", ""IsAssisted"", ""IsWeighted"", ""TaxonomySignals"", ""Confidence"", ""CreatedAtUtc"", ""UpdatedAtUtc"" FROM exercise_catalogs;");` (confirmar nomes exatos das colunas físicas no banco — podem estar em snake_case dependendo do naming convention configurado no `AwakenDbContext`; checar uma migration existente de `exercise_catalogs` para copiar o naming certo).
  3. `migrationBuilder.DropColumn` para cada uma das 16 colunas em `exercise_catalogs` — **isto é o que elimina a duplicação**: depois desta migration, o dado mora só em `exercise_taxonomies`.
  4. `Down()` faz o caminho inverso (recria as colunas em `exercise_catalogs`, copia de volta, dropa `exercise_taxonomies`).

```csharp
// ExerciseTaxonomy.cs
public class ExerciseTaxonomy : BaseEntity
{
    public Guid ExerciseCatalogId { get; private set; }
    public string MovementFamily { get; private set; } = string.Empty;
    public string MovementPattern { get; private set; } = string.Empty;
    public string Mechanic { get; private set; } = string.Empty;
    public string ForceType { get; private set; } = string.Empty;
    public string PlaneOfMotion { get; private set; } = string.Empty;
    public string Laterality { get; private set; } = string.Empty;
    public string BodyPosition { get; private set; } = string.Empty;
    public string? BenchAngle { get; private set; }
    public string EquipmentCategory { get; private set; } = string.Empty;
    public string LoadType { get; private set; } = string.Empty;
    public string PrimaryRegion { get; private set; } = string.Empty;
    public bool IsCompound { get; private set; }
    public bool IsUnilateral { get; private set; }
    public bool IsAssisted { get; private set; }
    public bool IsWeighted { get; private set; }
    public List<string> Signals { get; private set; } = [];
    public string Confidence { get; private set; } = "medium";

    private ExerciseTaxonomy() { }

    public static ExerciseTaxonomy Create(Guid exerciseCatalogId, ExerciseTaxonomySnapshot snapshot) => new()
    {
        ExerciseCatalogId = exerciseCatalogId,
        MovementFamily = snapshot.MovementFamily,
        MovementPattern = snapshot.MovementPattern,
        Mechanic = snapshot.Mechanic,
        ForceType = snapshot.ForceType,
        PlaneOfMotion = snapshot.PlaneOfMotion,
        Laterality = snapshot.Laterality,
        BodyPosition = snapshot.BodyPosition,
        BenchAngle = snapshot.BenchAngle,
        EquipmentCategory = snapshot.EquipmentCategory,
        LoadType = snapshot.LoadType,
        PrimaryRegion = snapshot.PrimaryRegion,
        IsCompound = snapshot.IsCompound,
        IsUnilateral = snapshot.IsUnilateral,
        IsAssisted = snapshot.IsAssisted,
        IsWeighted = snapshot.IsWeighted,
        Signals = snapshot.Signals,
        Confidence = snapshot.Confidence,
    };

    public void Apply(ExerciseTaxonomySnapshot snapshot, DateTime utcNow)
    {
        MovementFamily = snapshot.MovementFamily;
        MovementPattern = snapshot.MovementPattern;
        Mechanic = snapshot.Mechanic;
        ForceType = snapshot.ForceType;
        PlaneOfMotion = snapshot.PlaneOfMotion;
        Laterality = snapshot.Laterality;
        BodyPosition = snapshot.BodyPosition;
        BenchAngle = snapshot.BenchAngle;
        EquipmentCategory = snapshot.EquipmentCategory;
        LoadType = snapshot.LoadType;
        PrimaryRegion = snapshot.PrimaryRegion;
        IsCompound = snapshot.IsCompound;
        IsUnilateral = snapshot.IsUnilateral;
        IsAssisted = snapshot.IsAssisted;
        IsWeighted = snapshot.IsWeighted;
        Signals = snapshot.Signals;
        Confidence = snapshot.Confidence;
        UpdatedAtUtc = utcNow;
    }
}

public record ExerciseTaxonomySnapshot(
    string MovementFamily, string MovementPattern, string Mechanic, string ForceType,
    string PlaneOfMotion, string Laterality, string BodyPosition, string? BenchAngle,
    string EquipmentCategory, string LoadType, string PrimaryRegion,
    bool IsCompound, bool IsUnilateral, bool IsAssisted, bool IsWeighted,
    List<string> Signals, string Confidence);
```

Em `ExerciseCatalog.cs`, as 16 propriedades saem de campos próprios e viram (sem coluna, sem duplicação de armazenamento):

```csharp
public ExerciseTaxonomy? Taxonomy { get; private set; }

public string MovementPattern => Taxonomy?.MovementPattern ?? string.Empty;
public string MovementFamily => Taxonomy?.MovementFamily ?? string.Empty;
public string Mechanic => Taxonomy?.Mechanic ?? string.Empty;
public string ForceType => Taxonomy?.ForceType ?? string.Empty;
public string PlaneOfMotion => Taxonomy?.PlaneOfMotion ?? string.Empty;
public string Laterality => Taxonomy?.Laterality ?? string.Empty;
public string BodyPosition => Taxonomy?.BodyPosition ?? string.Empty;
public string? BenchAngle => Taxonomy?.BenchAngle;
public string EquipmentCategory => Taxonomy?.EquipmentCategory ?? string.Empty;
public string LoadType => Taxonomy?.LoadType ?? string.Empty;
public string PrimaryRegion => Taxonomy?.PrimaryRegion ?? string.Empty;
public bool IsCompound => Taxonomy?.IsCompound ?? false;
public bool IsUnilateral => Taxonomy?.IsUnilateral ?? false;
public bool IsAssisted => Taxonomy?.IsAssisted ?? false;
public bool IsWeighted => Taxonomy?.IsWeighted ?? false;
public List<string> TaxonomySignals => Taxonomy?.Signals ?? [];
public string Confidence => Taxonomy?.Confidence ?? "medium";
```

E dentro de `Apply(ExerciseCatalogSnapshot snapshot, DateTime utcNow)` (o método já existente), no lugar das 16 atribuições diretas removidas, criar/atualizar o filho:

```csharp
var taxonomySnapshot = new ExerciseTaxonomySnapshot(
    snapshot.MovementFamily, snapshot.MovementPattern, snapshot.Mechanic, snapshot.ForceType,
    snapshot.PlaneOfMotion, snapshot.Laterality, snapshot.BodyPosition, snapshot.BenchAngle,
    snapshot.EquipmentCategory, snapshot.LoadType, snapshot.PrimaryRegion,
    snapshot.IsCompound, snapshot.IsUnilateral, snapshot.IsAssisted, snapshot.IsWeighted,
    snapshot.TaxonomySignals, snapshot.Confidence);

if (Taxonomy is null)
    Taxonomy = ExerciseTaxonomy.Create(Id, taxonomySnapshot);
else
    Taxonomy.Apply(taxonomySnapshot, utcNow);
```

`ExerciseCatalogSnapshot` (o record) **não muda de forma** — `ImportExercisesCommandHandler.BuildCatalogSnapshot` continua construindo os mesmos 60+ campos de sempre; só o que o `Apply()` faz por dentro muda. Isso mantém o diff do handler de import mínimo.

**Confirmar antes de implementar:** que `BaseEntity` atribui `Id = Guid.NewGuid()` no construtor (não só no `SaveChanges`), para que `ExerciseTaxonomy.Create(Id, ...)` funcione com um `ExerciseCatalog` ainda não persistido. Se `Id` só é populado pelo EF ao salvar, ajustar para usar `HasForeignKey<ExerciseTaxonomy>` com shadow FK resolvida automaticamente pelo EF via navegação (`builder.Entity(...).HasOne(...).WithOne()` sem depender do `Id` estar setado manualmente) — neste caso, adicionar o `Taxonomy` à entidade basta, o EF resolve a FK no `SaveChanges`.

- [ ] **Step 1:** Escrever teste `ExerciseCatalogTests` (ou similar): given snapshot com `MovementPattern="squat"`, when `ExerciseCatalog.Create(snapshot, utcNow)`, then `catalog.Taxonomy` não é nulo e `catalog.MovementPattern == "squat"` (via getter delegado).
- [ ] **Step 2:** Rodar, confirmar falha (campo ainda não existe/estrutura antiga).
- [ ] **Step 3:** Implementar `ExerciseTaxonomy` + mudanças em `ExerciseCatalog`/config/DbContext.
- [ ] **Step 4:** Rodar `dotnet build` — corrigir qualquer consumidor que dependia de um *setter* público desses campos (não deveria haver, já eram `private set`, mas confirmar).
- [ ] **Step 5:** Rodar a suíte inteira (`dotnet test`), confirmar que nada quebrou (getters delegados preservam o comportamento de leitura).
- [ ] **Step 6:** Gerar e aplicar a migration de extração+backfill localmente, confirmar dado migrado corretamente (`SELECT * FROM exercise_taxonomies LIMIT 5` bate com o que estava antes em `exercise_catalogs`).

### 1.1 — `IMediaStorageService` + implementação R2

**Files:**
- Create: `backend/src/Awaken.Application/Common/Interfaces/IMediaStorageService.cs`
- Create: `backend/src/Awaken.Infrastructure/Services/CloudflareR2MediaStorageService.cs`
- Modify: `backend/src/Awaken.Infrastructure/DependencyInjection.cs`
- Test: `backend/tests/Awaken.UnitTests/Infrastructure/CloudflareR2MediaStorageServiceTests.cs`

```csharp
// IMediaStorageService.cs
namespace Awaken.Application.Common.Interfaces;

public interface IMediaStorageService
{
    /// Envia o conteúdo para o storage de mídia e retorna a URL pública resultante.
    Task<string> UploadAsync(string key, Stream content, string contentType, CancellationToken cancellationToken = default);
}
```

Implementação usa `AmazonS3Client` configurado com endpoint R2 (`https://{R2AccountId}.r2.cloudflarestorage.com`), lendo `Cloudflare:R2AccountId/R2AccessKey/R2SecretKey/R2BucketName` de `IConfiguration` (já existem em `appsettings.json`/`.env.example`, hoje vazios/não consumidos). `PutObjectRequest` com `CannedACL = S3CannedACL.PublicRead` (ou o bucket já é público — confirmar contrato do R2Bucket; se o bucket não é público, retornar a URL pública configurada via `Cloudflare:R2PublicBaseUrl` — adicionar essa chave a appsettings se necessário). Retorna `$"{publicBaseUrl}/{key}"`.

Registrar em `DependencyInjection.cs`: `services.AddSingleton<IMediaStorageService, CloudflareR2MediaStorageService>();`

- [ ] **Step 1:** Escrever teste que verifica que `UploadAsync` chama `PutObjectAsync` com a key/bucket corretos e retorna a URL esperada (usar `Mock<IAmazonS3>` injetado via um construtor que aceite `IAmazonS3` diretamente, para não precisar de credenciais reais em teste).
- [ ] **Step 2:** Rodar teste, ver falhar (classe não existe).
- [ ] **Step 3:** Implementar `IMediaStorageService`/`CloudflareR2MediaStorageService` conforme acima.
- [ ] **Step 4:** Rodar teste, ver passar.
- [ ] **Step 5:** Commit (task própria — mas lembrando: **não commitar nesta sessão**, conforme pedido do usuário; apenas deixar o working tree pronto).

### 1.2 — Renomear `ExerciseCatalogRelation` → `ExerciseRelationship` + `DatasetVersion`/`SourceFile` rastreáveis + upload real no handler

**Rename, não duplicação:** deletar `ExerciseCatalogRelation.cs`/`ExerciseCatalogRelationConfiguration.cs`; criar `ExerciseRelationship.cs`/`ExerciseRelationshipConfiguration.cs` com o mesmo conteúdo + `DatasetVersion`. A migration usa `RenameTable` (não `CreateTable` + `DropTable`), preservando os dados já importados.

**Files:**
- Delete: `backend/src/Awaken.Domain/Entities/Exercises/ExerciseCatalogRelation.cs`
- Create: `backend/src/Awaken.Domain/Entities/Exercises/ExerciseRelationship.cs` — mesma classe (`ExerciseCatalogId`, `RelatedProviderExerciseId`, `RelatedName`, `RelationKind`, `Types`, `Score`, `Confidence`, `Reasons`) renomeada, + `public string? DatasetVersion { get; private set; }` e parâmetro novo (nullable) em `Create(...)`.
- Delete: `backend/src/Awaken.Infrastructure/Persistence/Configurations/ExerciseCatalogRelationConfiguration.cs`
- Create: `backend/src/Awaken.Infrastructure/Persistence/Configurations/ExerciseRelationshipConfiguration.cs` — mesmo mapeamento (tabela `exercise_relationships`, índice composto `(ExerciseCatalogId, RelationKind)`, índice em `RelatedProviderExerciseId`) + coluna `DatasetVersion` (`MaxLength(32)`, nullable).
- Modify: `backend/src/Awaken.Domain/Entities/Exercises/ExerciseCatalog.cs` — `List<ExerciseRelationship> Relations` (tipo renomeado), `ReplaceRelations(IEnumerable<ExerciseRelationship> relations, ...)`.
- Modify: `backend/src/Awaken.Infrastructure/Persistence/Configurations/ExerciseCatalogConfiguration.cs` — `builder.HasMany(e => e.Relations)` continua igual, só o tipo genérico muda.
- Modify: `backend/src/Awaken.Infrastructure/Persistence/AwakenDbContext.cs` — `DbSet<ExerciseRelationship> ExerciseRelationships` (renomeado de `ExerciseCatalogRelations`).
- Modify: `backend/src/Awaken.Domain/Entities/Exercises/ExerciseRawImport.cs` — adicionar `string? DatasetVersion` como parâmetro opcional em `CreateImported`/`CreateFailed` (mantendo compatibilidade — parâmetro novo no fim, nullable).
- Modify: `backend/src/Awaken.Application/Exercises/Commands/ImportExercises/ImportExercisesCommandHandler.cs` — `BuildRelations` passa a retornar `IEnumerable<ExerciseRelationship>` e chamar `ExerciseRelationship.Create(...)`.
- Modify: `backend/src/Awaken.Application/Exercises/Commands/ImportExercises/ImportExercisesCommand.cs` — adicionar `string? DatasetVersion` ao record.
- Migration: `backend/src/Awaken.Infrastructure/Persistence/Migrations/20260706HHMMSS_RenameExerciseCatalogRelationToExerciseRelationship.cs` — `migrationBuilder.RenameTable(name: "exercise_catalog_relations", newName: "exercise_relationships"); migrationBuilder.AddColumn<string>(name: "DatasetVersion", table: "exercise_relationships", maxLength: 32, nullable: true);` (+ `Down()` inverso).
- Modify: `backend/src/Awaken.Contracts/Exercises/ImportExercisesRequest.cs` / `ImportExercisesResponse.cs` — adicionar `DatasetVersion` (request) e `MediaUploaded`/`Pending` (response, ver tabela de contrato da US-236 §17).
- Modify: `backend/src/Awaken.Application/Exercises/Commands/ImportExercises/ImportExercisesCommandHandler.cs`:
  - Injetar `IMediaStorageService mediaStorageService`.
  - Onde hoje `mediaKey` é só uma string relativa (linha ~62-64), fazer upload real quando `mediaPath is not null`: `var gifUrl = await mediaStorageService.UploadAsync($"exercises/{providerExerciseId}/360.gif", File.OpenRead(mediaPath), "image/gif", cancellationToken);` — capturar falha de upload separadamente (não falhar o item inteiro; se upload falhar, `gifUrl = null`, seguir sem mídia — RN de US-236 §9.2 diz que só bloqueia *aprovação*, não o import).
  - Passar `gifUrl` (em vez do `mediaKey` relativo) para `BuildCatalogSnapshot` como valor de `GifUrl`.
  - Passar `request.DatasetVersion` para `ExerciseRawImport.CreateImported/CreateFailed` e para cada `ExerciseRelationship.Create`.
  - Incrementar um novo contador `mediaUploaded` quando o upload ocorre com sucesso; retornar em `ImportExercisesResponse`.
- Migration: `backend/src/Awaken.Infrastructure/Persistence/Migrations/20260706HHMMSS_AddDatasetVersionToImports.cs` — `ALTER TABLE exercise_raw_imports ADD COLUMN "DatasetVersion" varchar(32) NULL; ALTER TABLE exercise_catalog_relations ADD COLUMN "DatasetVersion" varchar(32) NULL;` (gerar via `dotnet ef migrations add`, não escrever SQL à mão — este é schema puro, não seed, então usar o fluxo normal `dotnet ef migrations add AddDatasetVersionToImports -p Awaken.Infrastructure -s Awaken.Api`).

**Test scenarios (xUnit, seguir convenção de `ImportExercisesCommandHandlerTests` se já existir; senão criar `backend/tests/Awaken.UnitTests/Exercises/ImportExercisesCommandHandlerTests.cs` com Mocks para `IExerciseRawImportRepository`, `IExerciseCatalogRepository`, `IUnitOfWork`, `IDateTimeService`, `ISafeDirectoryResolver`, `IExerciseCatalogCacheService`, `IMediaStorageService`):**

| Cenário (US-236) | Given | When | Then |
|---|---|---|---|
| CA-001 taxonomia aplicada | item com `taxonomy.*` completo casado por `id` | import roda | `ExerciseCatalog` criado com todos os campos de taxonomia preenchidos (já coberto pelos testes existentes de `BuildCatalogSnapshot`; só confirmar que não regrediu) |
| CA-002 múltiplos candidatos | item com 3 `substitutions` de scores diferentes | import roda | 3 `ExerciseRelationship` persistidas, nenhuma descartada, `DatasetVersion` setado em todas |
| CA-003 mídia obrigatória | exercício sem gif encontrado (`FindMediaFile` retorna null) e sem `gifUrl` no JSON | `ApproveForWorkoutGeneration` é chamado (via `ApproveOnImport=true`) | `CanBeApproved()` retorna `false`; `IsApprovedForWorkoutGeneration` permanece `false`; `rawImport.MarkPendingReview` é chamado, não `MarkApproved` |
| CA-004 sem correspondência | processa arquivo cujo `id` não bate com nenhum catálogo pré-existente | import roda | comportamento atual já cobre (cria catálogo novo) — cenário real de "pendência" é quando o item é do dataset enriquecido mas *não existe ainda* nenhum `ExerciseRawImport`/`ExerciseCatalog` prévio: como o handler cria ambos do zero, não há "pendência" real neste pipeline unificado — documentar que este fluxo sempre cria, nunca fica pendente (depende de já existir builder simples) |
| upload de mídia | `mediaPath` resolvido | import roda | `mediaStorageService.UploadAsync` chamado com key `exercises/{id}/360.gif`, `GifUrl` do catálogo = URL retornada pelo mock, `mediaUploaded` incrementado |
| upload falha | `mediaStorageService.UploadAsync` lança exceção | import roda | item não conta como `failed` (RN: falha de mídia não derruba o item); `GifUrl` fica `null`; item segue como `pending_review` |

- [ ] Escrever os testes acima (extremos primeiro: CA-003 e "upload falha").
- [ ] Rodar `dotnet test --filter FullyQualifiedName~ImportExercisesCommandHandlerTests`, confirmar falha.
- [ ] Implementar as mudanças do handler descritas acima.
- [ ] Rodar de novo, confirmar passo.

### 1.3 — `CanBeApproved()` exige GIF

**Files:**
- Modify: `backend/src/Awaken.Domain/Entities/Exercises/ExerciseCatalog.cs:142-151`

```csharp
public bool CanBeApproved() =>
    InstructionsPtBr.Count > 0
    && PrimaryMuscleGroups.Count > 0
    && AttributeContribution is not null
    && AttributeContribution.IsValidForApprovedExercise()
    && !string.IsNullOrWhiteSpace(GifUrl); // RN-006 (US-236): GIF 360 é obrigatório, reforça RN-EPIC-005-005.
```

- [ ] **Step 1:** Rodar a suíte completa (`dotnet test`) ANTES de mexer, anotar quantos testes hoje dependem de `CanBeApproved()` aceitando só Video/Image (grep por `CanBeApproved`, `ApproveForWorkoutGeneration`, `VideoUrl`/`ImageUrl` em contexto de teste de aprovação).
- [ ] **Step 2:** Aplicar a mudança acima.
- [ ] **Step 3:** Rodar `dotnet test` de novo; para cada teste quebrado, ajustar o *setup do teste* (adicionar `GifUrl` de teste ao snapshot) — não a asserção do teste, a menos que a asserção literalmente testasse "aprova com só vídeo", caso em que o teste deve ser atualizado para refletir a nova regra (documentar no corpo do teste por quê).

### 1.4 — Leitura de relações: query + endpoint

**Files:**
- Create: `backend/src/Awaken.Application/Exercises/Queries/GetExerciseRelationships/GetExerciseRelationshipsQuery.cs`
- Create: `backend/src/Awaken.Application/Exercises/Queries/GetExerciseRelationships/GetExerciseRelationshipsQueryHandler.cs`
- Create: `backend/src/Awaken.Contracts/Exercises/ExerciseRelationshipResponse.cs`
- Modify: `backend/src/Awaken.Domain/Repositories/IExerciseCatalogRepository.cs` — adicionar `Task<ExerciseCatalog?> GetByProviderExerciseIdWithRelationsAsync(string providerName, string providerExerciseId, CancellationToken ct = default);` (confirmar primeiro se `GetByProviderExerciseIdAsync` já faz `.Include(e => e.Relations)` no `ExerciseCatalogRepository.cs` de infra — se já fizer, não precisa de método novo, só usar o existente).
- Modify: `backend/src/Awaken.Api/Controllers/V1/ExercisesController.cs` (checar se existe; se só existir `AdminExercisesController`, criar `ExercisesController` pública com `[Authorize]` simples).

```csharp
public record GetExerciseRelationshipsQuery(string ProviderExerciseId, string? Category)
    : IRequest<IReadOnlyList<ExerciseRelationshipResponse>>;

public record ExerciseRelationshipResponse(
    string ExerciseId,
    string Name,
    string RelationCategory,
    IReadOnlyList<string> Types,
    decimal Score,
    string Confidence);
```

Handler: busca o `ExerciseCatalog` de origem por `ProviderExerciseId`, filtra `Relations` por `RelationKind == Category` (se informado), ordena por `Score` desc, mapeia. Se `Category` vier nulo, retorna todas as categorias juntas ordenadas por score.

Endpoint: `GET /api/exercises/{providerExerciseId}/relationships?category=substitution` → `mediator.Send(new GetExerciseRelationshipsQuery(providerExerciseId, category))`.

**Test scenarios:**
- Given exercício com 3 relações (substitution score 90/70/50), When query sem `category`, Then retorna as 3 ordenadas desc por score.
- Given exercício com relações de categorias mistas, When `category=progression`, Then retorna só as `progression`.
- Given `providerExerciseId` inexistente, When query, Then lança `NotFoundException` (padrão do repo, confirmar no `GetExerciseCatalogQueryHandler` existente).

- [ ] Escrever os 3 testes do handler (Mock de `IExerciseCatalogRepository`).
- [ ] Rodar, confirmar falha.
- [ ] Implementar query/handler/response/controller.
- [ ] Rodar, confirmar passo.

### 1.5 — Flutter: exibir GIF + seção de alternativas (somente leitura)

**Convenções confirmadas:** exercício exibido via `Image.network` dentro de `Dialog` (ver `pre_quest_exercise_card.dart:47-62` e `exercise_execution_page.dart` `_showDemo`); campo hoje só `videoUrl`; sem `cached_network_image`; DTOs são hand-written sem codegen; testes usam fake repository + `mocktail` + `Key('...')`.

**Files:**
- Modify: `apps/mobile/lib/features/quests/domain/entities/quest_preview.dart` — adicionar `final String? gifUrl;` em `QuestPreviewExercise`.
- Modify: `apps/mobile/lib/features/quests/domain/entities/quest_execution.dart` — adicionar `final String? gifUrl;` em `QuestExecutionExercise`.
- Modify: `apps/mobile/lib/features/quests/data/dtos/quest_execution_response_dto.dart` e `quest_response_dto.dart` — parsear `gifUrl` do JSON (backend já envia `gifUrl` na serialização do workout, ver `WorkoutGeneratorService.cs:102`).
- Modify: `pre_quest_exercise_card.dart` e `exercise_execution_page.dart` — no `Image.network`, preferir `exercise.gifUrl ?? exercise.videoUrl` como source.
- Create: `apps/mobile/lib/features/quests/domain/entities/exercise_relationship.dart` — `class ExerciseRelationship { final String exerciseId; final String name; final String relationCategory; final List<String> types; final double score; final String confidence; }`.
- Create: `apps/mobile/lib/features/quests/data/datasources/exercise_relationships_data_source.dart` — `GET /api/exercises/{id}/relationships` via Dio (seguir padrão de outro `*_remote_data_source.dart`).
- Create: `apps/mobile/lib/features/quests/presentation/widgets/exercise_alternatives_sheet.dart` — bottom sheet somente leitura, lista candidatos com badge de tipo (usar chaves i18n para o rótulo do tipo — nunca renderizar `reasons` cru, RN-008 da US-236) e score. Abrir a partir de um novo botão/link na tela de detalhe (`_showExerciseDetails`/`_showDemo`), ex.: `TextButton(key: Key('exercise-alternatives-link'), ...)`.
- Modify: `apps/mobile/lib/l10n/app_{pt,en,es,fr}.arb` — chaves novas: `exerciseAlternativesTitle`, `exerciseAlternativesEmpty`, `exerciseRelationTypeSimilar`, `exerciseRelationTypeSubstitution`, `exerciseRelationTypeProgression`, `exerciseRelationTypeRegression` (seguir o padrão `"@key": {"description": "..."}` de todas as outras chaves).

**Test scenarios (widget/provider tests, seguir padrão de `quest_execution_page_test.dart`):**
- Given `ExerciseExecutionPage` com exercício cujo `gifUrl` não é nulo, When renderiza, Then `Image.network` usa a URL do gif (não a de vídeo).
- Given lista de 2 relações retornada por um fake data source, When abre `ExerciseAlternativesSheet`, Then mostra 2 itens ordenados por score, nenhum texto em inglês de `reasons` visível.
- Given data source retorna lista vazia, When abre o sheet, Then mostra `exerciseAlternativesEmpty`.

- [ ] Escrever os 3 testes acima (ou os que fizerem sentido dado o fake/mocktail já usado no arquivo de referência).
- [ ] Rodar `flutter test`, confirmar falha.
- [ ] Implementar entidades/DTOs/data source/widget conforme acima.
- [ ] Rodar `flutter test` + `flutter analyze`, confirmar passo/limpo.

---

## Task Group 2 — US-237: Split map (Agente B, Onda 1)

**Convenções confirmadas:** entidades em `Awaken.Domain/Entities/Training/`; seed via migration dedicada com SQL bruto + GUIDs fixos (ver `20260702095822_SeedInitialTrainingPrograms.cs`); repositório simples sem cache Redis (`TrainingProgramRepository.cs`); controller `TrainingProgramsController.cs` (`GET /api/training-programs`); Flutter em `features/training_programs/`.

### 2.1 — Constantes de grupos musculares e padrões de movimento

**Files:**
- Create: `backend/src/Awaken.Domain/Entities/Training/MuscleGroups.cs`
- Create: `backend/src/Awaken.Domain/Entities/Training/MovementPatterns.cs`

```csharp
// MuscleGroups.cs
namespace Awaken.Domain.Entities.Training;

public static class MuscleGroups
{
    public const string Chest = "chest";
    public const string Back = "back";
    public const string Shoulders = "shoulders";
    public const string Biceps = "biceps";
    public const string Triceps = "triceps";
    public const string Forearms = "forearms";
    public const string Traps = "traps";
    public const string RearDelts = "rear_delts";
    public const string Quadriceps = "quadriceps";
    public const string Hamstrings = "hamstrings";
    public const string Glutes = "glutes";
    public const string Calves = "calves";
    public const string Core = "core";

    public static readonly IReadOnlySet<string> All = new HashSet<string>(StringComparer.Ordinal)
    {
        Chest, Back, Shoulders, Biceps, Triceps, Forearms, Traps, RearDelts,
        Quadriceps, Hamstrings, Glutes, Calves, Core,
    };

    public static bool IsValid(string value) => All.Contains(value);
}
```

```csharp
// MovementPatterns.cs — lista canônica da seção 7 do README do EPIC-005.
namespace Awaken.Domain.Entities.Training;

public static class MovementPatterns
{
    public const string Squat = "squat";
    public const string Hinge = "hinge";
    public const string HorizontalPush = "horizontal_push";
    public const string VerticalPush = "vertical_push";
    public const string HorizontalPull = "horizontal_pull";
    public const string VerticalPull = "vertical_pull";
    public const string Lunge = "lunge";
    public const string Carry = "carry";
    public const string CoreFlexion = "core_flexion";
    public const string CoreAntiExtension = "core_anti_extension";
    public const string CoreAntiRotation = "core_anti_rotation";
    public const string Locomotion = "locomotion";
    public const string Jump = "jump";
    public const string Balance = "balance";
    public const string Mobility = "mobility";

    public static readonly IReadOnlySet<string> All = new HashSet<string>(StringComparer.Ordinal)
    {
        Squat, Hinge, HorizontalPush, VerticalPush, HorizontalPull, VerticalPull,
        Lunge, Carry, CoreFlexion, CoreAntiExtension, CoreAntiRotation, Locomotion, Jump, Balance, Mobility,
    };

    public static bool IsValid(string value) => All.Contains(value);
}
```

- [ ] Escrever teste `MuscleGroupsTests`/`MovementPatternsTests` simples: `IsValid("chest")` true, `IsValid("bogus")` false.
- [ ] Rodar, falhar.
- [ ] Implementar as duas classes.
- [ ] Rodar, passar.

### 2.2 — Entidades `TrainingProgramSplit`/`TrainingSplitDay` + validação + repositório

**Files:**
- Create: `backend/src/Awaken.Domain/Entities/Training/TrainingProgramSplit.cs`
- Create: `backend/src/Awaken.Domain/Entities/Training/TrainingSplitDay.cs`
- Create: `backend/src/Awaken.Domain/Repositories/ITrainingProgramSplitRepository.cs`
- Create: `backend/src/Awaken.Infrastructure/Persistence/Repositories/TrainingProgramSplitRepository.cs`
- Create: `backend/src/Awaken.Infrastructure/Persistence/Configurations/TrainingProgramSplitConfiguration.cs`
- Create: `backend/src/Awaken.Infrastructure/Persistence/Configurations/TrainingSplitDayConfiguration.cs`
- Modify: `backend/src/Awaken.Infrastructure/Persistence/AwakenDbContext.cs` — `DbSet<TrainingProgramSplit> TrainingProgramSplits`, `DbSet<TrainingSplitDay> TrainingSplitDays`.
- Modify: `backend/src/Awaken.Infrastructure/DependencyInjection.cs` — registrar `ITrainingProgramSplitRepository`.
- Test: `backend/tests/Awaken.UnitTests/Training/TrainingProgramSplitTests.cs`

```csharp
// TrainingProgramSplit.cs
public class TrainingProgramSplit : BaseEntity
{
    public string ProgramKey { get; private set; } = string.Empty;
    public string SplitMapVersion { get; private set; } = string.Empty;
    public int DayCount { get; private set; }
    public bool IsActive { get; private set; } = true;
    private readonly List<TrainingSplitDay> _days = [];
    public IReadOnlyList<TrainingSplitDay> Days => _days.AsReadOnly();

    private TrainingProgramSplit() { }

    public static TrainingProgramSplit Create(string programKey, string splitMapVersion, IEnumerable<TrainingSplitDaySeed> days)
    {
        var daysList = days.ToList();
        if (daysList.Count == 0)
            throw new InvalidOperationException($"Split map de '{programKey}' precisa de ao menos 1 dia."); // RN-002

        var split = new TrainingProgramSplit
        {
            ProgramKey = programKey,
            SplitMapVersion = splitMapVersion,
            DayCount = daysList.Count,
        };

        var index = 1;
        foreach (var day in daysList)
        {
            if (day.TargetMuscleGroups.Count == 0 || day.TargetMovementPatterns.Count == 0)
                throw new InvalidOperationException($"Dia '{day.DayKey}' precisa de ao menos 1 grupo e 1 padrão-alvo."); // RN-002

            foreach (var group in day.TargetMuscleGroups.Concat(day.SecondaryMuscleGroups))
                if (!MuscleGroups.IsValid(group))
                    throw new InvalidOperationException($"Grupo muscular inválido: '{group}'."); // RN-005

            foreach (var pattern in day.TargetMovementPatterns)
                if (!MovementPatterns.IsValid(pattern))
                    throw new InvalidOperationException($"Padrão de movimento inválido: '{pattern}'."); // RN-005

            split._days.Add(TrainingSplitDay.Create(index, day));
            index++;
        }

        return split;
    }
}

public record TrainingSplitDaySeed(
    string DayKey,
    string LabelI18nKey,
    string Role,
    IReadOnlyList<string> TargetMuscleGroups,
    IReadOnlyList<string> SecondaryMuscleGroups,
    IReadOnlyList<string> TargetMovementPatterns,
    bool AllowsCoreFinisher,
    int MinExercises,
    int MaxExercises);
```

```csharp
// TrainingSplitDay.cs
public class TrainingSplitDay : BaseEntity
{
    public Guid TrainingProgramSplitId { get; private set; }
    public int DayIndex { get; private set; }
    public string DayKey { get; private set; } = string.Empty;
    public string LabelI18nKey { get; private set; } = string.Empty;
    public string Role { get; private set; } = string.Empty;
    public List<string> TargetMuscleGroups { get; private set; } = [];
    public List<string> SecondaryMuscleGroups { get; private set; } = [];
    public List<string> TargetMovementPatterns { get; private set; } = [];
    public bool AllowsCoreFinisher { get; private set; }
    public int MinExercises { get; private set; }
    public int MaxExercises { get; private set; }

    private TrainingSplitDay() { }

    internal static TrainingSplitDay Create(int dayIndex, TrainingSplitDaySeed seed) => new()
    {
        DayIndex = dayIndex,
        DayKey = seed.DayKey,
        LabelI18nKey = seed.LabelI18nKey,
        Role = seed.Role,
        TargetMuscleGroups = seed.TargetMuscleGroups.ToList(),
        SecondaryMuscleGroups = seed.SecondaryMuscleGroups.ToList(),
        TargetMovementPatterns = seed.TargetMovementPatterns.ToList(),
        AllowsCoreFinisher = seed.AllowsCoreFinisher,
        MinExercises = seed.MinExercises,
        MaxExercises = seed.MaxExercises,
    };
}
```

`ITrainingProgramSplitRepository`:
```csharp
public interface ITrainingProgramSplitRepository
{
    Task<TrainingProgramSplit?> GetByProgramKeyAsync(string programKey, CancellationToken ct = default);
}
```

Config (seguir `TrainingProgramConfiguration.cs`): tabelas `training_program_splits` (unique index em `ProgramKey`) e `training_split_days` (FK `TrainingProgramSplitId`, index composto `(TrainingProgramSplitId, DayIndex)` unique). `TargetMuscleGroups`/`SecondaryMuscleGroups`/`TargetMovementPatterns` mapeados como `text[]` nativo do Npgsql (mesmo padrão de `ExerciseRelationshipConfiguration`, sem `HasConversion`).

**Test scenarios:**
| CA | Given | When | Then |
|---|---|---|---|
| CA-001 | seeds válidos dos 5 programas | `TrainingProgramSplit.Create` | `full_body`=1 dia, `ab`=2, `abc`=3, `abcd`=4, `abcde`=5 |
| CA-002 | seed do `ab` com dia A tendo `quadriceps` e dia B tendo `hamstrings`/`glutes` | `Create` | ambos passam sem exceção (pernas integradas) |
| CA-003 | seed com grupo `"bogus_muscle"` | `Create` | lança `InvalidOperationException` |
| RN-002 | seed de um dia sem `TargetMuscleGroups` | `Create` | lança `InvalidOperationException` |

- [ ] Escrever os 4 testes.
- [ ] Rodar, falhar.
- [ ] Implementar `TrainingProgramSplit`/`TrainingSplitDay`/repositório/config/DbSet/DI.
- [ ] Rodar, passar.

### 2.3 — Seed determinístico dos 5 programas clássicos

**Files:**
- Migration schema: `dotnet ef migrations add AddTrainingProgramSplit -p Awaken.Infrastructure -s Awaken.Api` (gera `CreateTable` para as duas tabelas).
- Migration seed: `backend/src/Awaken.Infrastructure/Persistence/Migrations/20260706HHMMSS_SeedInitialTrainingProgramSplits.cs` — copiar EXATAMENTE o padrão de `20260702095822_SeedInitialTrainingPrograms.cs` (GUIDs fixos por linha, `SqlGuid/SqlString/SqlBool/SqlNullableInt` helpers, `INSERT ... ON CONFLICT DO NOTHING`, `SeedUtcNow` fixo).

Dados a semear (direto da seção 6 da US-237 — copiar literalmente):

- `full_body` v1, 1 dia: `FB` role=`full_body`, label=`programDayFullBody`, grupos=[chest,back,quadriceps,hamstrings,glutes,shoulders,core], padrões=[horizontal_push,vertical_push,horizontal_pull,vertical_pull,squat,lunge,hinge,core_flexion], allowsCoreFinisher=false (já é o próprio corpo todo), min/max exercises=5/7.
- `ab` v1, 2 dias: A role=`push` label=`programDayPush` grupos=[chest,shoulders,triceps,quadriceps,calves] padrões=[horizontal_push,vertical_push,squat,lunge,core_flexion] allowsCoreFinisher=true min/max=4/6; B role=`pull` label=`programDayPull` grupos=[back,biceps,rear_delts,hamstrings,glutes] padrões=[horizontal_pull,vertical_pull,hinge,core_flexion] allowsCoreFinisher=true min/max=4/6.
- `abc` v1, 3 dias: A push [chest,shoulders,triceps] [horizontal_push,vertical_push,core_flexion] core=true 4/6; B pull [back,biceps,rear_delts,traps] [horizontal_pull,vertical_pull] core=false 4/6; C legs [quadriceps,hamstrings,glutes,calves,core] [squat,hinge,lunge,core_flexion] core=true 4/6.
- `abcd` v1, 4 dias: A chest [chest,triceps] [horizontal_push,vertical_push] core=false 3/5; B back [back,biceps,rear_delts] [horizontal_pull,vertical_pull] core=false 3/5; C legs [quadriceps,hamstrings,glutes,calves] [squat,hinge,lunge] core=false 3/5; D shoulders [shoulders,traps,core] [vertical_push,carry,core_flexion] core=true 3/5.
- `abcde` v1, 5 dias: A chest [chest,triceps] [horizontal_push,vertical_push] core=false 3/5; B back [back,traps,rear_delts] [horizontal_pull,vertical_pull] core=false 3/5; C legs [quadriceps,hamstrings,glutes,calves] [squat,hinge,lunge] core=false 3/5; D shoulders [shoulders,traps] [vertical_push] core=false 3/5; E arms [biceps,triceps,forearms,core] [core_flexion] core=true 3/5.

`perfect_2`/`system` **não** recebem split (RN-009) — não inserir linhas para eles.

- [ ] Rodar `dotnet ef migrations add AddTrainingProgramSplit ...`, inspecionar o `Up()` gerado.
- [ ] Escrever a migration de seed com os dados acima (GUIDs fixos, ex.: `d1000001-...` a `d1000005-...` para os splits, `d2000001-...` em diante para os dias).
- [ ] `dotnet ef database update -p Awaken.Infrastructure -s Awaken.Api` local, conferir no banco que as 5 linhas + N dias existem.
- [ ] Teste de integração leve (se a suíte já usa Testcontainers para isso, seguir o padrão; senão, um teste de repositório simples contra o DbContext in-memory/SQLite é aceitável) confirmando `GetByProgramKeyAsync("abc")` retorna 3 dias na ordem certa.

### 2.4 — Query + endpoint `GET /api/training-programs/{programKey}/split`

**Files:**
- Create: `backend/src/Awaken.Application/TrainingPrograms/Queries/GetTrainingProgramSplit/GetTrainingProgramSplitQuery.cs`
- Create: `.../GetTrainingProgramSplitQueryHandler.cs`
- Create: `backend/src/Awaken.Contracts/TrainingPrograms/TrainingProgramSplitResponse.cs`
- Modify: `backend/src/Awaken.Api/Controllers/V1/TrainingProgramsController.cs`

```csharp
public record GetTrainingProgramSplitQuery(string ProgramKey) : IRequest<TrainingProgramSplitResponse>;

public record TrainingProgramSplitResponse(
    string ProgramKey,
    string SplitMapVersion,
    IReadOnlyList<TrainingSplitDayResponse> Days);

public record TrainingSplitDayResponse(
    string DayKey,
    string Role,
    string LabelI18nKey,
    IReadOnlyList<string> TargetMuscleGroups,
    IReadOnlyList<string> TargetMovementPatterns,
    bool AllowsCoreFinisher);
```

Handler lança `NotFoundException` se `programKey` não tiver split (ex.: `perfect_2`/`system`) — controller deixa isso virar 404 (padrão já usado em outros handlers, confirmar middleware de exceção existente).

Endpoint: `[HttpGet("{programKey}/split")]` na `TrainingProgramsController` existente.

**Test scenarios:** Given `abc` semeado, When query, Then retorna 3 dias na ordem A/B/C com os campos certos. Given `perfect_2`, When query, Then `NotFoundException`.

- [ ] Escrever os 2 testes do handler.
- [ ] Rodar, falhar.
- [ ] Implementar.
- [ ] Rodar, passar.

### 2.5 — Flutter: split real na tela de programas

**Files:**
- Create: `apps/mobile/lib/features/training_programs/domain/entities/training_program_split.dart` — `TrainingProgramSplitDay { dayKey, role, labelI18nKey, targetMuscleGroups, targetMovementPatterns, allowsCoreFinisher }`.
- Modify: `apps/mobile/lib/features/training_programs/data/datasources/training_programs_remote_data_source.dart` — adicionar `Future<List<TrainingProgramSplitDay>> getSplit(String programKey)` chamando `GET /api/training-programs/{programKey}/split`.
- Modify: `apps/mobile/lib/features/training_programs/presentation/pages/training_programs_page.dart` — na área expandida do card (`_expanded`), em vez de só mostrar `description`, buscar e mostrar a lista de dias reais (rótulo i18n do dia + grupos musculares), com fallback ao texto de `description` se a chamada falhar/404 (`perfect_2`/`system`).
- Modify: `apps/mobile/lib/l10n/app_{pt,en,es,fr}.arb` — chaves `programDayFullBody`, `programDayPush`, `programDayPull`, `programDayLegs`, `programDayChest`, `programDayBack`, `programDayShoulders`, `programDayArms`, e um rótulo por grupo muscular usado na UI (`muscleGroupChest`, `muscleGroupBack`, etc., se ainda não existirem — checar antes de duplicar).

**Test scenarios:** Given fake data source retorna 3 dias para `abc`, When expande o card do programa `abc`, Then mostra as 3 letras/rótulos e os grupos musculares, nos 3 idiomas testados (pelo menos pt/en via `Locale`).

- [ ] Escrever o teste de widget (seguir `training_programs_page` já deve ter teste — confirmar path exato e completar).
- [ ] Rodar, falhar.
- [ ] Implementar entidade/data source/página/ARB.
- [ ] Rodar `flutter test` + `flutter analyze`.

---

## Task Group 3 — US-238: Rotação do dia (Agente C, Onda 2, depende da Task 2.2/2.4)

### 3.1 — `DailyProgramDayResolver` (serviço de domínio puro)

**Files:**
- Create: `backend/src/Awaken.Domain/Services/Training/DailyProgramDayResolver.cs`
- Test: `backend/tests/Awaken.UnitTests/Training/DailyProgramDayResolverTests.cs`

```csharp
namespace Awaken.Domain.Services.Training;

public static class DailyProgramDayResolver
{
    public static DailyProgramDayResolution Resolve(IReadOnlyList<string> days, string? lastCompletedDayKey)
    {
        if (days.Count == 0)
            throw new InvalidOperationException("Programa sem dias no split map.");

        if (days.Count == 1)
            return new DailyProgramDayResolution(days[0], 1, "full_body_single_day");

        if (lastCompletedDayKey is null)
            return new DailyProgramDayResolution(days[0], 1, "first_workout");

        var lastIndex = days.ToList().FindIndex(d => string.Equals(d, lastCompletedDayKey, StringComparison.Ordinal));
        if (lastIndex < 0)
            return new DailyProgramDayResolution(days[0], 1, "program_changed");

        var nextIndex = (lastIndex + 1) % days.Count;
        return new DailyProgramDayResolution(days[nextIndex], nextIndex + 1, "cyclic_successor");
    }
}

public record DailyProgramDayResolution(string DayKey, int DayIndex, string Reason);
```

**Test scenarios (direto das CAs da US-238, nomear os testes com o ID da CA):**

```csharp
public class DailyProgramDayResolverTests
{
    [Fact]
    public void CA001_AbcdeUltimoC_ResolveD()
    {
        var result = DailyProgramDayResolver.Resolve(["A", "B", "C", "D", "E"], "C");
        result.DayKey.Should().Be("D");
        result.DayIndex.Should().Be(4);
        result.Reason.Should().Be("cyclic_successor");
    }

    [Fact]
    public void CA002_AbUltimoB_VoltaParaA()
    {
        var result = DailyProgramDayResolver.Resolve(["A", "B"], "B");
        result.DayKey.Should().Be("A");
        result.Reason.Should().Be("cyclic_successor");
    }

    [Fact]
    public void CA003_SemHistorico_PrimeiroDia()
    {
        var result = DailyProgramDayResolver.Resolve(["A", "B", "C"], null);
        result.DayKey.Should().Be("A");
        result.DayIndex.Should().Be(1);
        result.Reason.Should().Be("first_workout");
    }

    [Fact]
    public void CA005_TrocaDePrograma_LastKeyNaoPerteceAosDias_ReiniciaNoPrimeiro()
    {
        var result = DailyProgramDayResolver.Resolve(["A", "B", "C", "D"], "C"); // "C" era do ABC antigo, novo programa é ABCD com dias diferentes na prática seria outro lastKey inexistente — simular com key fora do novo conjunto:
        var resultAfterSwitch = DailyProgramDayResolver.Resolve(["A", "B", "C", "D"], "Z");
        resultAfterSwitch.DayKey.Should().Be("A");
        resultAfterSwitch.Reason.Should().Be("program_changed");
    }

    [Fact]
    public void CA006_FullBody_SempreResolveUnicoDia()
    {
        var result = DailyProgramDayResolver.Resolve(["FB"], "FB");
        result.DayKey.Should().Be("FB");
        result.Reason.Should().Be("full_body_single_day");
    }

    [Fact]
    public void RN007_FolgaLonga_ContinuaDoUltimoConcluido()
    {
        // Independente de quanto tempo se passou, o resolver só olha lastCompletedDayKey — nunca a data.
        var result = DailyProgramDayResolver.Resolve(["A", "B", "C"], "A");
        result.DayKey.Should().Be("B");
    }
}
```

- [ ] Escrever os 6 testes acima.
- [ ] Rodar `dotnet test --filter DailyProgramDayResolverTests`, confirmar falha (classe não existe).
- [ ] Implementar `DailyProgramDayResolver`.
- [ ] Rodar, confirmar passo.

### 3.2 — Persistência do dia resolvido no `Quest` + busca do último concluído

**Files:**
- Modify: `backend/src/Awaken.Domain/Entities/Quests/Quest.cs` — novos campos e método:

```csharp
public string? ResolvedProgramKey { get; private set; }
public string? ResolvedDayKey { get; private set; }
public int? ResolvedDayIndex { get; private set; }
public string? SplitMapVersion { get; private set; }

/// US-238: registra o dia do programa resolvido pela rotação para esta geração.
public void AssignResolvedProgramDay(string programKey, string dayKey, int dayIndex, string splitMapVersion, DateTime utcNow)
{
    ResolvedProgramKey = programKey;
    ResolvedDayKey = dayKey;
    ResolvedDayIndex = dayIndex;
    SplitMapVersion = splitMapVersion;
    UpdatedAtUtc = utcNow;
}
```

- Modify: `backend/src/Awaken.Domain/Repositories/IQuestRepository.cs` — adicionar:
```csharp
/// US-238: último Quest concluído do usuário no mesmo programa, para a rotação cíclica.
Task<Quest?> GetLastCompletedByUserAndProgramAsync(
    Guid userId, string programKey, CancellationToken cancellationToken = default);
```
- Modify: `backend/src/Awaken.Infrastructure/Persistence/Repositories/QuestRepository.cs` — implementar com `Where(q => q.UserId == userId && q.TrainingType == "program" && q.ProgramId == programKey && q.Status == "completed").OrderByDescending(q => q.CompletedAtUtc).FirstOrDefaultAsync(...)`.
- Modify: `backend/src/Awaken.Infrastructure/Persistence/Configurations/QuestConfiguration.cs` (confirmar nome exato do arquivo) — mapear as 4 colunas novas (nullable), e um índice `(UserId, ProgramId, CompletedAtUtc)` para a query acima performar (RN citada na doc §14).
- Migration: `dotnet ef migrations add AddResolvedProgramDayToQuests -p Awaken.Infrastructure -s Awaken.Api`.

**Test scenarios:**
- `QuestTests` (ou arquivo de teste de `Quest` se já existir): Given quest nova, When `AssignResolvedProgramDay("abc", "B", 2, "v1", now)`, Then os 4 campos batem e `UpdatedAtUtc == now`.
- `QuestRepositoryTests` (se a suíte já tiver testes de repositório com DB in-memory/Testcontainers — seguir o padrão existente): Given 2 quests completadas do mesmo usuário/programa em datas diferentes + 1 de outro programa, When `GetLastCompletedByUserAndProgramAsync`, Then retorna a mais recente do programa certo, ignorando a de outro programa e as não concluídas.

- [ ] Escrever os testes.
- [ ] Rodar, falhar.
- [ ] Implementar campos/método/repositório/config/migration.
- [ ] Rodar, passar.

### 3.3 — Query/endpoint de auditoria `GET /api/quests/resolve-day` (opcional, mas pedido no §18 da US-238)

**Files:**
- Create: `backend/src/Awaken.Application/Quests/Queries/GetResolvedProgramDay/GetResolvedProgramDayQuery.cs` (+ handler)
- Create: `backend/src/Awaken.Contracts/Quests/ResolvedProgramDayResponse.cs`
- Modify: controller de quests existente (encontrar `QuestsController.cs`, adicionar rota).

Handler: resolve `programKey` ativo do usuário atual (via `IUserWorkoutPreferenceRepository`, já existente), busca dias via `ITrainingProgramSplitRepository`, busca último concluído via `IQuestRepository.GetLastCompletedByUserAndProgramAsync`, chama `DailyProgramDayResolver.Resolve`, monta a resposta conforme o exemplo do §18 da US-238.

- [ ] Teste do handler: given preferência = "abcde", último concluído dayKey "C", when Handle, then retorna `resolvedDayKey="D"`, `reason="cyclic_successor"`.
- [ ] Rodar, falhar, implementar, passar.

---

## Task Group 4 — US-239: Recuperação muscular (Agente D, Onda 2, paralelo à Task Group 3)

### 4.1 — Entidade `MuscleRecoveryState` + repositório

**Files:**
- Create: `backend/src/Awaken.Domain/Entities/Training/MuscleRecoveryState.cs`
- Create: `backend/src/Awaken.Domain/Repositories/IMuscleRecoveryStateRepository.cs`
- Create: `backend/src/Awaken.Infrastructure/Persistence/Repositories/MuscleRecoveryStateRepository.cs`
- Create: `backend/src/Awaken.Infrastructure/Persistence/Configurations/MuscleRecoveryStateConfiguration.cs`
- Modify: `AwakenDbContext.cs`, `DependencyInjection.cs`

```csharp
public class MuscleRecoveryState : BaseEntity
{
    public Guid UserId { get; private set; }
    public string MuscleGroup { get; private set; } = string.Empty;
    public DateTime LastTrainedAtUtc { get; private set; }
    public string LastIntensity { get; private set; } = "moderate"; // light|moderate|heavy
    public List<string> LastMovementFamilies { get; private set; } = [];
    public int WeeklySetsAccumulated { get; private set; }
    public DateTime WeekAnchorDateUtc { get; private set; }

    private MuscleRecoveryState() { }

    public static MuscleRecoveryState CreateInitial(Guid userId, string muscleGroup, DateTime utcNow) => new()
    {
        UserId = userId,
        MuscleGroup = muscleGroup,
        LastTrainedAtUtc = utcNow,
        WeekAnchorDateUtc = StartOfWeek(utcNow),
    };

    /// RN-008: só treinos concluídos chamam isto.
    public void RegisterSession(DateTime utcNow, string intensity, IReadOnlyCollection<string> movementFamilies, int setsPerformed)
    {
        if (StartOfWeek(utcNow) != WeekAnchorDateUtc)
        {
            WeeklySetsAccumulated = 0;
            WeekAnchorDateUtc = StartOfWeek(utcNow);
        }

        LastTrainedAtUtc = utcNow;
        LastIntensity = intensity;
        LastMovementFamilies = movementFamilies.ToList();
        WeeklySetsAccumulated += setsPerformed;
        UpdatedAtUtc = utcNow;
    }

    private static DateTime StartOfWeek(DateTime utc) =>
        utc.Date.AddDays(-(int)utc.DayOfWeek);
}
```

```csharp
public interface IMuscleRecoveryStateRepository
{
    Task<IReadOnlyList<MuscleRecoveryState>> GetByUserIdAsync(Guid userId, CancellationToken ct = default);
    Task<MuscleRecoveryState?> GetByUserAndMuscleGroupAsync(Guid userId, string muscleGroup, CancellationToken ct = default);
    Task AddAsync(MuscleRecoveryState state, CancellationToken ct = default);
    void Update(MuscleRecoveryState state);
}
```

Config: tabela `muscle_recovery_states`, unique index `(UserId, MuscleGroup)`.

- [ ] Teste: `RegisterSession` acumula `WeeklySetsAccumulated` dentro da mesma semana e reseta ao virar a semana (`WeekAnchorDateUtc` muda).
- [ ] Rodar, falhar, implementar, passar.

### 4.2 — `RecoveryPlanner` (serviço de domínio puro, tabelas científicas)

**Files:**
- Create: `backend/src/Awaken.Domain/Services/Training/RecoveryPlanner.cs`
- Test: `backend/tests/Awaken.UnitTests/Training/RecoveryPlannerTests.cs`

```csharp
namespace Awaken.Domain.Services.Training;

public static class RecoveryPlanner
{
    // Seção 6.1 da US-239: janela mínima por intensidade da última sessão.
    private static readonly Dictionary<string, int> RecoveryWindowHours = new(StringComparer.Ordinal)
    {
        ["light"] = 24,
        ["moderate"] = 48,
        ["heavy"] = 72,
    };

    // Seção 6.2 da US-239: teto de séries semanais recuperáveis por nível efetivo.
    private static readonly Dictionary<string, (int Min, int Max, int PerSessionCap)> WeeklyVolumeByLevel = new(StringComparer.OrdinalIgnoreCase)
    {
        ["sedentary"] = (6, 10, 2),
        ["beginner"] = (8, 12, 3),
        ["intermediate"] = (12, 16, 4),
        ["advanced"] = (14, 20, 5),
    };

    public static MuscleRecoveryStatus StatusFor(MuscleRecoveryState? state, DateTime utcNow)
    {
        if (state is null)
            return MuscleRecoveryStatus.Recovered;

        var windowHours = RecoveryWindowHours.GetValueOrDefault(state.LastIntensity, 48);
        var hoursSince = (utcNow - state.LastTrainedAtUtc).TotalHours;

        if (hoursSince >= windowHours)
            return MuscleRecoveryStatus.Recovered;

        // Muito abaixo da janela (< 50% do tempo necessário) e volume semanal já alto = fadigado.
        return hoursSince < windowHours * 0.5
            ? MuscleRecoveryStatus.Fatigued
            : MuscleRecoveryStatus.Recovering;
    }

    public static (decimal VolumeCapFactor, int RpeCapDelta) ModulationFor(MuscleRecoveryStatus status) => status switch
    {
        MuscleRecoveryStatus.Recovered => (1.0m, 0),
        MuscleRecoveryStatus.Recovering => (0.5m, -1),
        MuscleRecoveryStatus.Fatigued => (0.25m, -2),
        _ => (1.0m, 0),
    };

    public static int WeeklySetCapFor(string effectiveExperienceLevel) =>
        WeeklyVolumeByLevel.TryGetValue(effectiveExperienceLevel, out var range) ? range.Max : WeeklyVolumeByLevel["sedentary"].Max;

    public static int PerSessionSetCapFor(string effectiveExperienceLevel) =>
        WeeklyVolumeByLevel.TryGetValue(effectiveExperienceLevel, out var range) ? range.PerSessionCap : WeeklyVolumeByLevel["sedentary"].PerSessionCap;
}

public enum MuscleRecoveryStatus { Recovered, Recovering, Fatigued }
```

**Test scenarios (nomeados por CA/RN da US-239):**

| Teste | Given | When | Then |
|---|---|---|---|
| `CA001_PernasPesadoOntem_VolumeRebaixado` | `LastIntensity="heavy"`, `LastTrainedAtUtc` = 20h atrás | `StatusFor` | `Recovering` (dentro da janela de 72h, mas > metade) → `ModulationFor` dá `0.5m`/`-1` |
| `RN001_JanelaLeve24h_JaPassou_Recuperado` | `LastIntensity="light"`, treinado há 30h | `StatusFor` | `Recovered` |
| `RN003_Fadigado_CapMinimo` | `LastIntensity="heavy"`, treinado há 10h (< 36h = metade de 72h) | `StatusFor` | `Fatigued` → `ModulationFor` dá `0.25m`/`-2` |
| `CA004_TetoSemanal_Avancado` | nível `advanced` | `WeeklySetCapFor` | `20` |
| sem histórico | `state = null` | `StatusFor` | `Recovered` (RN 10.4 — retorno após folga longa/sem estado prévio) |

- [ ] Escrever os 5 testes.
- [ ] Rodar, falhar.
- [ ] Implementar `RecoveryPlanner`.
- [ ] Rodar, passar.

### 4.3 — Wiring: atualizar `MuscleRecoveryState` ao concluir quest

**Files:**
- Modify: `backend/src/Awaken.Application/Quests/Commands/CompleteQuest/CompleteQuestCommandHandler.cs` — após `quest.Complete(...)`, para cada `QuestExercise` concluído nesta chamada, resolver `ExerciseCatalog` (via `ExerciseCatalogProviderId`), pegar `PrimaryMuscleGroups`/`SecondaryMuscleGroups` + `MovementFamily`, e para cada grupo chamar `IMuscleRecoveryStateRepository.GetByUserAndMuscleGroupAsync` → `RegisterSession(...)` (criar via `CreateInitial` se não existir, senão `Update`). Intensidade (`light|moderate|heavy`) inferida por `ExerciseCatalog.DifficultyRank`/`ImpactLevel` (>=4 impacto ou difficultyRank>=3 → `heavy`; difficultyRank==0 → `light`; senão `moderate`) — função helper privada no handler ou no `RecoveryPlanner` (`static string IntensityFor(ExerciseCatalog exercise)`).
- Injetar `IMuscleRecoveryStateRepository` e `IExerciseCatalogRepository` (se ainda não injetado) no handler.

**Test scenarios:**
- Given quest com 1 exercício de `quadriceps`/`heavy`, When `CompleteQuestCommandHandler.Handle`, Then `IMuscleRecoveryStateRepository` recebe uma chamada de update com `LastIntensity="heavy"` para `quadriceps`.
- Given quest já sem exercícios com catálogo resolvido (fallback), When completa, Then nenhuma atualização de recovery ocorre (não derruba o fluxo).

- [ ] Escrever os 2 testes (Mock do novo repositório no `CompleteQuestCommandHandlerTests.cs` existente).
- [ ] Rodar, falhar.
- [ ] Implementar o wiring.
- [ ] Rodar toda a suíte de `CompleteQuestCommandHandlerTests`, corrigir qualquer regressão de mocks não configurados.

---

## Task Group 5 — US-240: Blueprint do dia (Agente E, Onda 3 — integra tudo)

**Pré-requisito:** Task Groups 1-4 mergeados/finalizados no working tree (mesma branch, sem commit).

### 5.1 — `DailyWorkoutBlueprintBuilder`

**Files:**
- Create: `backend/src/Awaken.Application/Quests/Common/DailyWorkoutBlueprintBuilder.cs` (fica em Application pois orquestra repositórios via interfaces, ao contrário dos outros serviços de domínio puros — ver `FitnessProfileSnapshot.cs` como precedente de "serviço de orquestração em Application/Quests/Common").
- Modify: `backend/src/Awaken.Domain/Entities/Quests/Quest.cs` — `public string? DailyWorkoutBlueprintJson { get; private set; }` + método `AssignDailyWorkoutBlueprint(string json, DateTime utcNow)`.
- Create: `backend/tests/Awaken.UnitTests/Quests/DailyWorkoutBlueprintBuilderTests.cs`

```csharp
public record DailyWorkoutBlueprintTargetGroup(
    string MuscleGroup, decimal Weight, int VolumeBudgetSets, int RpeMax, IReadOnlyList<string> AvoidMovementFamilies);

public record DailyWorkoutBlueprint(
    string ProgramKey,
    string ResolvedDayKey,
    bool RestDay,
    bool FallbackUsed,
    string SplitMapVersion,
    IReadOnlyList<DailyWorkoutBlueprintTargetGroup> TargetGroups,
    IReadOnlyList<string> TargetPatterns,
    int EligibleExerciseCount);

public class DailyWorkoutBlueprintBuilder(
    IUserWorkoutPreferenceRepository preferenceRepository,
    ITrainingProgramSplitRepository splitRepository,
    IQuestRepository questRepository,
    IMuscleRecoveryStateRepository recoveryRepository,
    IExerciseCatalogRepository catalogRepository,
    IDateTimeService dateTimeService)
{
    private const int DefaultVolumeBudgetSets = 3;
    private const int DefaultRpeMax = 9;

    public async Task<DailyWorkoutBlueprint> BuildAsync(Guid userId, string effectiveExperienceLevel, CancellationToken ct = default)
    {
        var preference = await preferenceRepository.GetByUserIdAsync(userId, ct);
        // RN-006: sem programa selecionado, default por nível — sedentário/iniciante → full_body.
        var programKey = preference?.PreferredProgramId ?? TrainingProgramKeys.FullBody;

        var split = await splitRepository.GetByProgramKeyAsync(programKey, ct);
        if (split is null)
        {
            // RN-009: programa sem split clássico (perfect_2/system) — sem blueprint estruturado, fallback trivial.
            return new DailyWorkoutBlueprint(programKey, "N/A", RestDay: false, FallbackUsed: true, "n/a", [], [], 0);
        }

        var dayKeys = split.Days.Select(d => d.DayKey).ToList();
        var lastCompleted = await questRepository.GetLastCompletedByUserAndProgramAsync(userId, programKey, ct);
        var resolution = DailyProgramDayResolver.Resolve(dayKeys, lastCompleted?.ResolvedDayKey);
        var dayTarget = split.Days.First(d => d.DayKey == resolution.DayKey);

        var recoveryStates = await recoveryRepository.GetByUserIdAsync(userId, ct);
        var recoveryByGroup = recoveryStates.ToDictionary(s => s.MuscleGroup, StringComparer.Ordinal);
        var utcNow = dateTimeService.UtcNow;
        var perSessionCap = RecoveryPlanner.PerSessionSetCapFor(effectiveExperienceLevel);

        var targetGroups = dayTarget.TargetMuscleGroups.Select(group =>
        {
            var status = RecoveryPlanner.StatusFor(recoveryByGroup.GetValueOrDefault(group), utcNow);
            var (capFactor, rpeDelta) = RecoveryPlanner.ModulationFor(status);
            var avoidFamilies = recoveryByGroup.TryGetValue(group, out var state) ? state.LastMovementFamilies : [];
            return new DailyWorkoutBlueprintTargetGroup(
                MuscleGroup: group,
                Weight: dayTarget.SecondaryMuscleGroups.Contains(group) ? 0.6m : 1.0m,
                VolumeBudgetSets: Math.Max(1, (int)Math.Round(perSessionCap * capFactor)),
                RpeMax: Math.Clamp(DefaultRpeMax + rpeDelta, 5, DefaultRpeMax),
                AvoidMovementFamilies: avoidFamilies);
        }).ToList();

        var approvedCatalog = await catalogRepository.ListApprovedForWorkoutGenerationAsync(ct);
        var eligibleCount = approvedCatalog.Count(e =>
            e.PrimaryMuscleGroups.Any(g => dayTarget.TargetMuscleGroups.Contains(g)) &&
            dayTarget.TargetMovementPatterns.Contains(e.MovementPattern));

        // RN-007/US-046: sem elegíveis suficientes, aciona fallback em vez de treino incoerente.
        var fallbackUsed = eligibleCount < dayTarget.MinExercises;

        return new DailyWorkoutBlueprint(
            programKey, resolution.DayKey, RestDay: false, fallbackUsed, split.SplitMapVersion,
            targetGroups, dayTarget.TargetMovementPatterns, eligibleCount);
    }
}
```

- [ ] **Step 1:** Escrever os testes abaixo primeiro (Mocks para as 5 dependências).
- [ ] **Step 2:** Rodar, confirmar falha.
- [ ] **Step 3:** Implementar a classe.
- [ ] **Step 4:** Rodar, confirmar passo.

**Test scenarios (CAs da US-240):**

| CA | Given | When | Then |
|---|---|---|---|
| CA-001 | usuário no dia C (legs) do ABC, catálogo com exercícios de pernas e de peito | `BuildAsync` | `TargetPatterns` contém `squat/hinge/lunge/core_flexion`; `EligibleExerciseCount` conta só os de pernas coerentes |
| CA-002 | grupo `quadriceps` com `MuscleRecoveryState` recém-treinado pesado (status `Recovering`) | `BuildAsync` | `TargetGroups` para `quadriceps` tem `VolumeBudgetSets` reduzido e `RpeMax` menor que o default |
| CA-003 | `eligibleCount` abaixo de `dayTarget.MinExercises` | `BuildAsync` | `FallbackUsed == true` |
| CA-004 | `preference` é `null` (sem programa selecionado) | `BuildAsync` | `ProgramKey == "full_body"` |
| determinismo | mesma entrada (mocks fixos) | `BuildAsync` chamado 2x | resultado idêntico nas duas chamadas |

### 5.2 — Integração no `WorkoutGeneratorService`

**Files:**
- Modify: `backend/src/Awaken.Infrastructure/Services/WorkoutGeneratorService.cs`:
  - Injetar `DailyWorkoutBlueprintBuilder` (via `IServiceProvider`/construtor — como é `Awaken.Application`, referenciar diretamente).
  - Em `GenerateWorkoutJsonAsync`, ANTES do `ExerciseSafetyFilter.Apply`, chamar `blueprintBuilder.BuildAsync(userId, profile.EffectiveExperienceLevel, ct)`.
  - Filtrar `approvedExercises` para o subconjunto coerente com `blueprint.TargetGroups`/`TargetPatterns` **antes** de passar para `ExerciseSafetyFilter.Apply` (a segurança continua soberana e roda depois, RN-005 da US-240) — só filtrar quando `!blueprint.FallbackUsed`; se `FallbackUsed`, manter o catálogo completo aprovado como estava antes (comportamento atual preservado, sem regressão).
  - Após a seleção, aplicar o cap do blueprint no prescription final: para cada exercício selecionado, se `prescription.Sets > blueprintGroup.VolumeBudgetSets` para o grupo primário do exercício, usar `blueprintGroup.VolumeBudgetSets` no lugar de `prescription.Sets` na serialização.
  - Incluir `resolvedDayKey`, `resolvedProgramKey`, `splitMapVersion` no `catalogWorkout` serializado (novos campos) para o Flutter exibir.
  - Chamar `quest.AssignResolvedProgramDay(...)` e `quest.AssignDailyWorkoutBlueprint(...)` — isso precisa subir a interface `IWorkoutGeneratorService` para retornar esses dados também (`WorkoutGenerationResult` ganha `ResolvedProgramKey`, `ResolvedDayKey`, `ResolvedDayIndex`, `SplitMapVersion`, `DailyWorkoutBlueprintJson` como propriedades novas) e `GenerateDailyQuestCommandHandler` (e `RegenerateDailyQuestCommandHandler`) passam a chamar os dois métodos novos do `Quest` com esses valores.
- Modify: `backend/src/Awaken.Application/Common/Interfaces/IWorkoutGeneratorService.cs` — atualizar o record `WorkoutGenerationResult`.
- Modify: `backend/src/Awaken.Application/Quests/Commands/GenerateDailyQuest/GenerateDailyQuestCommandHandler.cs` e `RegenerateDailyQuestCommandHandler.cs` — chamar os novos métodos do `Quest` após `AssignWorkout`.

**Este é o ponto de maior risco de regressão.** Antes de mexer:
- [ ] Rodar a suíte inteira (`dotnet test`) e anotar o baseline (quantos passam hoje).
- [ ] Fazer a mudança.
- [ ] Rodar a suíte inteira de novo. Para CADA teste que quebrar em `GenerateDailyQuestCommandHandlerTests`, `RegenerateDailyQuestCommandHandlerTests`, `WorkoutGeneratorServiceTests`, `ExerciseSafetyFilterTests`, `ExerciseScoringEngineTests`, `ExerciseSelectionEngineTests`: ajustar o *setup do mock/fixture* (novo mock de `DailyWorkoutBlueprintBuilder`/repositórios) — não relaxar a asserção original a menos que ela testasse um comportamento que a US-240 explicitamente muda (documentar caso a caso no corpo do teste).
- [ ] Confirmar `dotnet test` 100% verde antes de finalizar a task.

### 5.3 — Flutter: rótulo do dia + aviso de recuperação

**Files:**
- Modify: `apps/mobile/lib/features/quests/data/dtos/quest_response_dto.dart` — parsear `resolvedDayKey`/`resolvedProgramKey` do workout JSON.
- Modify: `apps/mobile/lib/features/quests/presentation/pages/pre_quest_page.dart` — no `_PreviewHeroCard` (linha ~382-423), exibir o rótulo do dia (ex.: "Dia C — Pernas") quando `resolvedDayKey` não for nulo, usando as chaves i18n do split map (Task 2.5).
- Create: banner de "grupo em recuperação" seguindo o template de `exercise_feedback_banner.dart` (fade-in `AnimatedController`, `Container` com `AwakenColors.cardElevated`), exibido quando algum `targetGroup` do blueprint tiver `volumeCapFactor < 1.0` — texto via nova chave ARB `preQuestMuscleGroupRecoveryBanner`.
- Modify: ARB files — chaves novas.

**Test scenarios:** widget test em `pre_quest_page_test.dart` (confirmar se já existe; se não, criar seguindo o padrão do arquivo de referência já mapeado) — given workout com `resolvedDayKey="C"`, when renderiza, then mostra o rótulo do dia; given blueprint com grupo em recuperação, when renderiza, then mostra o banner.

- [ ] Escrever os testes.
- [ ] Rodar, falhar.
- [ ] Implementar.
- [ ] Rodar `flutter test`, `flutter analyze`.

### 5.4 — Passагem final de verificação (toda a epic)

- [ ] Backend: `dotnet build` limpo, `dotnet test` 100% verde (incluindo os testes de arquitetura NetArchTest, se existirem — confirmar que os novos serviços de domínio não referenciam `Awaken.Infrastructure`).
- [ ] Frontend: `flutter analyze` sem erros, `flutter test` 100% verde.
- [ ] `flutter gen-l10n` rodado após todas as mudanças de ARB.
- [ ] E2E manual/roteiro: conferir os critérios de "QA" de cada US (seções 20/21 de cada doc) que envolvem fluxo ponta a ponta (US-236 tela de detalhe+alternativas; US-237 tela de programas nos 3 idiomas; US-240 quest gerada refletindo dia/alvo).

---

## 3. Auto-revisão (cobertura da spec)

- US-236: `ExerciseTaxonomy` extraída como tabela própria ✓ (1.0), `ExerciseRelationship` (rename de `ExerciseCatalogRelation`) com múltiplos candidatos + leitura ✓ (1.2/1.4), mídia obrigatória/GIF 360 ✓ (1.3), upload real R2 ✓ (1.1/1.2), dataset version rastreável ✓ (1.2), Flutter GIF+alternativas ✓ (1.5). Divergência de classificação (RN-003) — decisão documentada de não implementar (seção 0), dado que a race condition não existe nesta arquitetura (pipeline único de import).
- US-237: split map dos 5 programas ✓ (2.2/2.3), validação de enum ✓ (2.1/2.2), coerência com US-231 ✓ (dados literais da doc), endpoint ✓ (2.4), Flutter ✓ (2.5).
- US-238: algoritmo de rotação ✓ (3.1), persistência/idempotência ✓ (3.2), endpoint de auditoria ✓ (3.3).
- US-239: estado de recuperação ✓ (4.1), tabelas científicas ✓ (4.2), wiring em conclusão de quest ✓ (4.3). Full body emphasis (alternância de ênfase em microciclo) — **gap consciente**: não incluído nesta rodada por ser um refinamento sobre `full_body` (RN-006/007 da US-239) que exige rastrear histórico de ênfase por sessão; deixar como follow-up explícito se o usuário quiser aprofundar (mencionar no resumo final).
- US-240: orquestrador ✓ (5.1), integração no pipeline ✓ (5.2), Flutter ✓ (5.3), fallback/rest day — `RestDay` sempre `false` nesta primeira versão (política de dia de descanso do §10.4 não foi detalhada na doc com regra determinística clara; **gap consciente**, mencionar no resumo final).

## 4. Desvios conscientes do texto literal da spec (resumo para comunicar ao usuário)

1. **`ExerciseTaxonomy` e `ExerciseRelationship` são criadas de verdade** (confirmado com o usuário), mas sem duplicar dado: `ExerciseTaxonomy` extrai (migra + dropa colunas) o que estava inline em `ExerciseCatalog`; `ExerciseRelationship` é o rename de `ExerciseCatalogRelation` (mesma tabela, dado preservado, `RenameTable` na migration), não uma tabela paralela. `ExerciseCatalog` mantém getters delegados (sem coluna própria) para não obrigar reescrever todo consumidor existente (scoring/selection engine, mapper).
2. Sem entidade `WorkoutSession` — usa `Quest`/`QuestExercise` já existentes, estendidos com campos novos (`ResolvedProgramKey/DayKey/DayIndex/SplitMapVersion/DailyWorkoutBlueprintJson`).
3. **`CanBeApproved()` passa a exigir `GifUrl` especificamente** (confirmado com o usuário) — antes aceitava vídeo/imagem/gif, qualquer um. Mudança de comportamento intencional pela RN-006/CA-003 da US-236; suíte inteira deve ser rodada e corrigida depois (Task 1.3).
4. Sem detecção de divergência de taxonomia (RN-003 US-236) — pipeline único de import elimina a race condition (dois escritores concorrentes) que a regra endereçava.
5. Full body "ênfase alternada por microciclo" (US-239) e política de "dia de descanso" (US-240 §10.4) implementados de forma simplificada/omitidos nesta rodada — sinalizar como follow-up.
