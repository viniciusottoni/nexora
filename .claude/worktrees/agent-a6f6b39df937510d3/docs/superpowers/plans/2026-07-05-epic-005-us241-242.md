# EPIC-005 — US-241 (Progressão Semanal Adaptativa) e US-242 (Orçamento de Tempo Determinístico) Implementation Plan

> **For agentic workers:** Execute task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Não faça `git commit`. Branch atual: `docs/epic-005-user-stories`. Rode `dotnet build` + `dotnet test backend/tests/Awaken.UnitTests` depois de cada task de backend, e `flutter analyze` + `flutter test` depois de cada task de Flutter. Corrija qualquer teste que quebrar antes de seguir para a próxima task.

**Goal:** Implementar US-242 (orçamento de tempo determinístico do treino) e US-241 (progressão semanal adaptativa), integrando ambos ao pipeline real de geração de quest (`WorkoutGeneratorService`/`DailyWorkoutBlueprintBuilder`/`ExercisePrescriptionEngine`), sem quebrar os 1551 testes de backend nem os testes Flutter existentes.

**Architecture:** Cada US vira um serviço de domínio puro e determinístico (static, sem I/O), seguindo o precedente já estabelecido em `ExercisePrescriptionEngine`/`RecoveryPlanner`/`ExerciseSelectionEngine`. A integração no pipeline (`WorkoutGeneratorService.GenerateWorkoutJsonAsync`) é **aditiva**: nada do que já existe (seleção por heurística de tempo, filtro de segurança, blueprint) é removido; US-242 roda como um passo de ajuste **depois** da seleção/prescrição atuais, e US-241 roda como um passo de **ajuste da prescrição** antes de aplicá-la aos exercícios selecionados. Ordem de implementação: **US-242 primeiro** (fundação de tempo, sem dependência de US-241), depois **US-241** (que usa `TimeBudgetCalculator` da US-242 para recalibrar quando o tempo muda).

**Tech Stack:** ASP.NET Core (.NET 10) + EF Core (Postgres/Npgsql) + MediatR/CQRS + FluentValidation, xUnit + FluentAssertions + Moq (backend); Flutter + Riverpod + go_router (frontend).

---

## 0. Descobertas de pesquisa que mudam o plano (decisões conscientes)

| Spec pede | Realidade do código | Decisão |
|---|---|---|
| `WorkoutTimeModel` como "config versionada" (tabela no banco, seção 14 da US-242) | Nenhum outro modelo determinístico do épico (`ExercisePrescriptionEngine`, `RecoveryPlanner`) usa tabela — todos são constantes estáticas no código. | **Não criar tabela.** `WorkoutTimeModel` é uma classe estática com `Version = "v1"` como constante. Versionar via deploy de código, igual ao resto do catálogo determinístico. O campo `WorkoutTimeModelVersion` ainda é persistido por quest (RN-007), só não há FK para uma tabela de config. |
| Unificar a duplicação de heurística de duração entre `ExerciseSafetyFilter.EstimatedDurationMinutes` e `ExerciseScoringEngine.EstimatedDurationMinutes` | Ambas são usadas **antes** da prescrição real existir (elegibilidade e scoring/pacote guloso rodam antes de `ExercisePrescriptionEngine.Prescribe`). Reescrever a ordem do pipeline pra usar sets/reps reais arrisca quebrar seleção/scoring já testados (dezenas de testes existentes). | **Não unificar agora.** As duas heurísticas continuam como pré-filtros aproximados internos (elegibilidade e ranking, nunca exibidos ao usuário). `TimeBudgetCalculator` (novo) vira a **única fonte de verdade** para o valor que é persistido/exibido (`estimatedDurationSeconds`) e para a resolução de conflito (RN-001/RN-002 da US-242) — roda **depois** da seleção+prescrição reais, como um passo de ajuste adicional. Documentado aqui para não ser confundido com regressão. |
| "Reps atingidas × meta" e "RPE médio × RPE-alvo" por exercício (US-241 §6.2) | `QuestExercise` só grava o que foi **prescrito** (Sets/RepsMin/RepsMax/TargetRpe) — nunca o que o usuário realmente fez ou sentiu. Não existe captura de desempenho real em lugar nenhum do app. | **Decisão do usuário (confirmada antes da execução):** considerar que o usuário sempre atinge o prescrito (reps = meta, sempre). O único sinal real capturado é **como o usuário se sentiu** ao final do treino (pergunta simples, 3 opções), perguntada na conclusão da quest. Isso substitui "RPE médio" por um proxy de 3 bandas: `too_easy` / `just_right` / `too_hard`. |
| Grafo de variante mais difícil/fácil (US-039/US-236) como eixo de progressão | Existe (`RegressionExerciseIds`/`ProgressionExerciseIds` em `ExerciseCatalog`), mas trocar o exercício selecionado por sua progressão exigiria reescrever a seleção (`ExerciseSelectionEngine`) para conhecer o plano semanal. | **Escopo reduzido, documentado:** o eixo `harder_variant`/`easier_variant` é **sinalizado** no `WeeklyProgressionPlan` (dado ao selecionar o próximo exercício, via bias de score) mas a troca efetiva de variante específica fica para os eixos que já têm alavanca direta hoje (reps/séries/descanso/RPE). Ver Task 6. |
| Viés por atributo (Agilidade → variantes complexas; Foco → tolerância a complexidade técnica) | Não há alavanca numérica pronta pra complexidade técnica sem tocar `ExerciseSafetyFilter`/`ExerciseScoringEngine` (mais blast radius). | **Implementado:** Força (reps−/RestSeconds igual, variantes mais pesadas → reduz RepsMin/Max), Resistência (reps+/descanso−), Vitalidade (+1 no teto de volume extra), Rank S+ (teto de RPE 9→10). **Não implementado nesta rodada:** Agilidade/Foco (sem alavanca numérica sem aumentar risco de regressão); documentado como gap conhecido no relatório final. |
| `WeeklyProgressionState` "recalibra imediatamente ao salvar mudança de perfil" (RN-006) | Tocar `UpdateUserProfileCommandHandler` pra disparar recálculo síncrono aumenta blast radius num handler crítico e testado. | **Recalibração por comparação de hash na próxima geração:** `WeeklyProgressionReviewer` compara um hash do perfil atual com `WeeklyProgressionState.ProfileSnapshotHash` toda vez que roda (a cada geração de quest). Diferente → recalibra e marca `RecalibratedFromProfileChange`. Efeito idêntico ao pedido pela RN ("recalibra para a próxima geração"), sem tocar `UpdateUserProfileCommandHandler`. |

---

## 1. Estrutura de arquivos (visão geral)

**Novo (backend, US-242):**
```
backend/src/Awaken.Domain/Services/Quests/WorkoutTimeModel.cs
backend/src/Awaken.Domain/Services/Quests/TimeBudgetCalculator.cs
backend/tests/Awaken.UnitTests/Quests/TimeBudgetCalculatorTests.cs
```

**Novo (backend, US-241):**
```
backend/src/Awaken.Domain/Entities/Progression/WeeklyProgressionState.cs
backend/src/Awaken.Domain/Repositories/IWeeklyProgressionStateRepository.cs
backend/src/Awaken.Infrastructure/Persistence/Repositories/WeeklyProgressionStateRepository.cs
backend/src/Awaken.Infrastructure/Persistence/Configurations/WeeklyProgressionStateConfiguration.cs
backend/src/Awaken.Domain/Services/Progression/WeeklyProgressionDecisionEngine.cs
backend/src/Awaken.Application/Progression/Common/WeeklyProgressionReviewer.cs
backend/src/Awaken.Application/Quests/Queries/GetWeeklyProgression/GetWeeklyProgressionQuery.cs
backend/src/Awaken.Application/Quests/Queries/GetWeeklyProgression/GetWeeklyProgressionQueryHandler.cs
backend/src/Awaken.Contracts/Quests/WeeklyProgressionResponse.cs
backend/tests/Awaken.UnitTests/Progression/WeeklyProgressionDecisionEngineTests.cs
backend/tests/Awaken.UnitTests/Progression/WeeklyProgressionReviewerTests.cs
backend/tests/Awaken.UnitTests/Quests/GetWeeklyProgressionQueryHandlerTests.cs
```

**Modificado (backend):**
```
backend/src/Awaken.Domain/Entities/Quests/Quest.cs                      (+campos tempo +campos progressão)
backend/src/Awaken.Domain/Entities/Quests/QuestLog.cs                   (+PerceivedFeeling)
backend/src/Awaken.Application/Common/Interfaces/IWorkoutGeneratorService.cs (+campos no WorkoutGenerationResult)
backend/src/Awaken.Infrastructure/Services/WorkoutGeneratorService.cs   (integra TimeBudgetCalculator + WeeklyProgressionPlan)
backend/src/Awaken.Application/Quests/Commands/GenerateDailyQuest/GenerateDailyQuestCommandHandler.cs (persiste novos campos)
backend/src/Awaken.Application/Quests/Commands/CompleteQuest/CompleteQuestCommand.cs      (+PerceivedFeeling)
backend/src/Awaken.Application/Quests/Commands/CompleteQuest/CompleteQuestCommandValidator.cs (valida enum)
backend/src/Awaken.Application/Quests/Commands/CompleteQuest/CompleteQuestCommandHandler.cs (passa PerceivedFeeling pro QuestLog)
backend/src/Awaken.Domain/Repositories/IQuestLogRepository.cs           (+GetCompletedSinceAsync)
backend/src/Awaken.Infrastructure/Persistence/Repositories/QuestLogRepository.cs
backend/src/Awaken.Application/Quests/Common/QuestResponseMapper.cs     (expõe novos campos no WorkoutDto)
backend/src/Awaken.Contracts/Quests/QuestResponse.cs                   (+campos no WorkoutDto/QuestPreviewResponse)
backend/src/Awaken.Contracts/Quests/QuestPreviewResponse.cs
backend/src/Awaken.Contracts/Quests/CompleteQuestRequest.cs (se existir; senão via query/body do controller)
backend/src/Awaken.Api/Controllers/V1/QuestsController.cs              (+endpoint GET weekly-progression, +body do complete)
backend/src/Awaken.Infrastructure/Persistence/AwakenDbContext.cs       (+DbSet<WeeklyProgressionState>)
backend/src/Awaken.Infrastructure/Persistence/Configurations/QuestConfiguration.cs
backend/src/Awaken.Infrastructure/Persistence/Configurations/QuestLogConfiguration.cs
backend/src/Awaken.Infrastructure/DependencyInjection.cs               (+registro WeeklyProgressionReviewer/repo)
backend/src/Awaken.Infrastructure/Persistence/Migrations/20260706*_AddTimeBudgetFieldsToQuests.cs
backend/src/Awaken.Infrastructure/Persistence/Migrations/20260706*_AddPerceivedFeelingToQuestLogs.cs
backend/src/Awaken.Infrastructure/Persistence/Migrations/20260706*_AddWeeklyProgressionState.cs
backend/tests/Awaken.UnitTests/Infrastructure/WorkoutGeneratorServiceTests.cs (ajustar fixtures quebradas pela nova etapa)
backend/tests/Awaken.UnitTests/Quests/CompleteQuestCommandHandlerTests.cs (novo caso PerceivedFeeling)
backend/tests/Awaken.UnitTests/Quests/GenerateDailyQuestCommandHandlerTests.cs (novos campos)
backend/tests/Awaken.IntegrationTests/QuestGenerationEndpointTests.cs / CompleteQuestEndpointTests.cs (ajustes)
```

**Novo/Modificado (Flutter):**
```
apps/mobile/lib/features/quests/domain/entities/quest_preview.dart          (+timeAdjustmentApplied, +isMicroQuest, +progressionMessageKey)
apps/mobile/lib/features/quests/data/dtos/quest_response_dto.dart           (parse novos campos)
apps/mobile/lib/features/quests/presentation/widgets/pre_quest_time_budget_banner.dart (novo)
apps/mobile/lib/features/quests/presentation/widgets/pre_quest_progression_banner.dart (novo)
apps/mobile/lib/features/quests/presentation/pages/pre_quest_page.dart      (renderiza os 2 banners novos)
apps/mobile/lib/features/quests/presentation/widgets/perceived_feeling_dialog.dart (novo)
apps/mobile/lib/features/quests/presentation/pages/quest_execution_page.dart (mostra o dialog antes de completar)
apps/mobile/lib/features/quests/presentation/providers/quest_execution_controller.dart (+parâmetro feeling em complete())
apps/mobile/lib/features/quests/data/repositories/quests_repository_impl.dart (+feeling em completeQuest)
apps/mobile/lib/l10n/app_{pt,en,es,fr}.arb (+chaves novas)
apps/mobile/test/features/quests/... (+testes novos/ajustados)
apps/mobile/test/e2e/pre_quest_flow_test.dart (ajuste se necessário)
```

---

## 2. Ondas de execução

```
Onda 1 (eu, sequencial): US-242 completo (fundação de tempo) — Tasks 1-4.
Onda 2 (eu, sequencial): US-241 completo (progressão semanal) — Tasks 5-11. Depende da Onda 1
  (usa TimeBudgetCalculator pra recalibrar quando o tempo muda, RN-008 da US-242).
Onda 3 (1 subagente em paralelo com a Onda 2, arquivos disjuntos — apps/mobile/**):
  Agente F → Task 12 (Flutter: banners de tempo/progressão + dialog de sentimento),
  disparado assim que o contrato de API (nomes de campo) da Task 3/9 estiver fechado.
Onda 4 (eu, sequencial): Task 13 — suíte completa (unit+integration+flutter), corrigir o que quebrar.
```

Por que não paralelizar US-241/US-242 em agentes diferentes: ambas tocam os mesmos arquivos centrais
(`Quest.cs`, `WorkoutGeneratorService.cs`, `GenerateDailyQuestCommandHandler.cs`, `QuestResponseMapper.cs`,
migrations, DI) — rodar em paralelo geraria conflito de merge garantido. US-242 é fundação de US-241
(RN-008), então a ordem sequencial já é a dependência real, não uma escolha arbitrária.

---

## Task 1 — `WorkoutTimeModel` (constantes determinísticas)

**Files:**
- Create: `backend/src/Awaken.Domain/Services/Quests/WorkoutTimeModel.cs`
- Test: `backend/tests/Awaken.UnitTests/Quests/TimeBudgetCalculatorTests.cs` (criado nesta task, populado na Task 2)

```csharp
namespace Awaken.Domain.Services.Quests;

/// <summary>
/// US-242 §6.1 — constantes do modelo de tempo determinístico. Versionado via
/// deploy de código (mesmo precedente de <see cref="ExercisePrescriptionEngine"/>/
/// <see cref="Awaken.Domain.Services.Training.RecoveryPlanner"/> — nenhum modelo
/// determinístico do catálogo usa tabela de config).
/// </summary>
public static class WorkoutTimeModel
{
    public const string Version = "v1";
    public const int SecondsPerRep = 3;
    public const int TransitionSeconds = 45;
    public const int CooldownSeconds = 120;
    public const int MicroQuestWarmupSeconds = 120;
    public const int MicroQuestThresholdMinutes = 15;
    public const decimal MinUtilization = 0.85m;

    public static int WarmupSecondsFor(string effectiveExperienceLevel) =>
        effectiveExperienceLevel.ToLowerInvariant() switch
        {
            "sedentary" or "beginner" => 300,
            "intermediate" => 420,
            "advanced" => 540,
            _ => 300,
        };
}
```

- [ ] **Step 1:** Criar o arquivo acima.
- [ ] **Step 2:** Criar o arquivo de teste vazio (será populado na Task 2) com o using básico:

```csharp
using Awaken.Domain.Services.Quests;
using FluentAssertions;
using Xunit;

namespace Awaken.UnitTests.Quests;

public class TimeBudgetCalculatorTests
{
}
```

- [ ] **Step 3:** Rodar `dotnet build` — deve compilar sem erros.

---

## Task 2 — `TimeBudgetCalculator` (fórmulas puras + ladder de resolução)

**Files:**
- Create/Modify: `backend/src/Awaken.Domain/Services/Quests/TimeBudgetCalculator.cs`
- Modify: `backend/tests/Awaken.UnitTests/Quests/TimeBudgetCalculatorTests.cs`

```csharp
namespace Awaken.Domain.Services.Quests;

/// <summary>
/// US-242: fórmula de duração estimada por exercício/quest (§6.2/6.3) e ladder
/// determinístico de resolução de conflito objetivo/intensidade × tempo (§6.5).
/// Serviço de domínio puro — opera só sobre os itens que o chamador já
/// selecionou/pontuou (<see cref="ExerciseSelectionEngine"/>/
/// <see cref="ExercisePrescriptionEngine"/>), sem I/O.
/// </summary>
public static class TimeBudgetCalculator
{
    private static readonly string[] DensityEligibleGoals =
        ["lose_weight", "fat_loss", "improve_conditioning", "conditioning"];

    /// US-242 §6.2: transição + (séries × execução por série) + descanso ENTRE séries.
    public static int ExerciseTimeCostSeconds(int sets, int reps, int? plannedDurationSeconds, int restSeconds)
    {
        var execPerSet = plannedDurationSeconds ?? reps * WorkoutTimeModel.SecondsPerRep;
        return WorkoutTimeModel.TransitionSeconds + sets * execPerSet + Math.Max(0, sets - 1) * restSeconds;
    }

    /// US-242 §6.3: aquecimento + soma dos timeCost + finalização.
    public static int EstimateQuestDurationSeconds(
        IEnumerable<int> exerciseTimeCostsSeconds, int warmupSeconds, int cooldownSeconds) =>
        warmupSeconds + exerciseTimeCostsSeconds.Sum() + cooldownSeconds;

    /// US-242 §6.5: aplica a ordem determinística de ajuste quando estoura o tempo
    /// disponível, ou adiciona volume quando sobra tempo (RN-002/RN-003/RN-004).
    public static TimeBudgetResolution Resolve(TimeBudgetRequest request)
    {
        var level = request.EffectiveExperienceLevel;
        var isMicroQuestByThreshold = request.AvailableMinutesPerWorkout <= WorkoutTimeModel.MicroQuestThresholdMinutes;

        var warmup = isMicroQuestByThreshold ? WorkoutTimeModel.MicroQuestWarmupSeconds : WorkoutTimeModel.WarmupSecondsFor(level);
        var cooldown = isMicroQuestByThreshold ? 0 : WorkoutTimeModel.CooldownSeconds;
        var adjustment = isMicroQuestByThreshold ? "micro_quest" : "none";
        var densityApplied = false;

        var availableSeconds = request.AvailableMinutesPerWorkout * 60;
        var softMinSeconds = (int)Math.Floor(availableSeconds * WorkoutTimeModel.MinUtilization);

        // Ordenado por prioridade decrescente: o fim da lista é cortado primeiro (RN-002).
        var items = request.SelectedItems.OrderByDescending(i => i.PriorityScore).ToList();

        int Cost(TimeBudgetItem i) => ExerciseTimeCostSeconds(i.Sets, i.Reps, i.PlannedDurationSeconds, i.RestSeconds);
        int Total() => EstimateQuestDurationSeconds(items.Select(Cost), warmup, cooldown);

        if (Total() > availableSeconds)
        {
            // 1) Reduzir quantidade de exercícios (menor prioridade primeiro), até o mínimo do dia.
            while (Total() > availableSeconds && items.Count > request.MinExerciseCount)
            {
                items.RemoveAt(items.Count - 1);
                adjustment = "reduced_exercises";
            }

            // 2) Reduzir séries em direção ao piso (nunca abaixo de 1) — preserva o descanso (RN-004).
            while (Total() > availableSeconds && items.Any(i => i.Sets > 1))
            {
                var index = items
                    .Select((item, idx) => (item, idx))
                    .Where(pair => pair.item.Sets > 1)
                    .OrderByDescending(pair => pair.item.Sets)
                    .ThenBy(pair => pair.item.PriorityScore)
                    .First().idx;
                items[index] = items[index] with { Sets = items[index].Sets - 1 };
                adjustment = "reduced_sets";
            }

            // 3) Densidade (só condicionamento/perda de peso) — reduz descanso efetivo.
            if (Total() > availableSeconds && DensityEligibleGoals.Contains(request.Goal, StringComparer.OrdinalIgnoreCase))
            {
                items = items.Select(i => i with { RestSeconds = Math.Max(15, i.RestSeconds / 2) }).ToList();
                densityApplied = true;
                adjustment = "density";
            }

            // 4) Micro quest — último recurso: aquecimento mínimo, sem finalização, mínimo de exercícios.
            if (Total() > availableSeconds)
            {
                warmup = WorkoutTimeModel.MicroQuestWarmupSeconds;
                cooldown = 0;
                while (Total() > availableSeconds && items.Count > 1)
                    items.RemoveAt(items.Count - 1);
                adjustment = "micro_quest";
            }
        }
        else if (Total() < softMinSeconds)
        {
            // RN-003: tempo sobrando — primeiro +séries (até o teto), depois +1 exercício.
            var addedSets = items.ToDictionary(i => i.ExerciseId, _ => 0);
            var addedAny = false;
            while (Total() < softMinSeconds && items.Any(i => addedSets[i.ExerciseId] < request.MaxAddedSetsPerExercise))
            {
                var candidate = items
                    .Where(i => addedSets[i.ExerciseId] < request.MaxAddedSetsPerExercise)
                    .OrderByDescending(i => i.PriorityScore)
                    .First();
                var index = items.FindIndex(i => i.ExerciseId == candidate.ExerciseId);
                var bumped = items[index] with { Sets = items[index].Sets + 1 };
                if (EstimateQuestDurationSeconds(
                        items.Select((i, idx) => idx == index ? Cost(bumped) : Cost(i)), warmup, cooldown) > availableSeconds)
                    break;

                items[index] = bumped;
                addedSets[candidate.ExerciseId]++;
                addedAny = true;
            }

            foreach (var extra in request.ExtraCandidates.OrderByDescending(i => i.PriorityScore))
            {
                if (Total() >= softMinSeconds) break;
                var candidateTotal = Total() + Cost(extra) + (items.Count == 0 ? 0 : 0);
                if (warmup + items.Select(Cost).Sum() + Cost(extra) + cooldown > availableSeconds) continue;
                items.Add(extra);
                addedAny = true;
            }

            if (addedAny) adjustment = "added_volume";
        }

        var finalTotal = Total();
        return new TimeBudgetResolution(
            Items: items,
            WarmupSeconds: warmup,
            CooldownSeconds: cooldown,
            EstimatedDurationSeconds: finalTotal,
            TimeBudgetSeconds: availableSeconds,
            Utilization: availableSeconds == 0 ? 0 : Math.Round((double)finalTotal / availableSeconds, 4),
            TimeAdjustmentApplied: adjustment,
            IsMicroQuest: isMicroQuestByThreshold || adjustment == "micro_quest",
            DensityApplied: densityApplied);
    }
}

/// <param name="PriorityScore">Score estático do exercício (US-151, `ExerciseScore.StaticScore`)
/// — usado para decidir ordem de corte/acréscimo. Maior = mantido primeiro.</param>
public sealed record TimeBudgetItem(
    string ExerciseId,
    int Sets,
    int Reps,
    int? PlannedDurationSeconds,
    int RestSeconds,
    double PriorityScore);

public sealed record TimeBudgetRequest(
    IReadOnlyList<TimeBudgetItem> SelectedItems,
    IReadOnlyList<TimeBudgetItem> ExtraCandidates,
    string EffectiveExperienceLevel,
    string? Goal,
    int AvailableMinutesPerWorkout,
    int MinExerciseCount = 1,
    int MaxAddedSetsPerExercise = 2);

public sealed record TimeBudgetResolution(
    IReadOnlyList<TimeBudgetItem> Items,
    int WarmupSeconds,
    int CooldownSeconds,
    int EstimatedDurationSeconds,
    int TimeBudgetSeconds,
    double Utilization,
    string TimeAdjustmentApplied,
    bool IsMicroQuest,
    bool DensityApplied);
```

- [ ] **Step 1:** Escrever os testes abaixo em `TimeBudgetCalculatorTests.cs` (substituindo a classe vazia da Task 1):

```csharp
using Awaken.Domain.Services.Quests;
using FluentAssertions;
using Xunit;

namespace Awaken.UnitTests.Quests;

public class TimeBudgetCalculatorTests
{
    [Fact]
    public void ExerciseTimeCostSeconds_SumsTransitionExecutionAndRestBetweenSets()
    {
        // 3 séries de 10 reps a 3s/rep = 30s/série; descanso 60s ENTRE séries (2×, não 3×); transição 45s.
        var cost = TimeBudgetCalculator.ExerciseTimeCostSeconds(sets: 3, reps: 10, plannedDurationSeconds: null, restSeconds: 60);
        cost.Should().Be(45 + 3 * 30 + 2 * 60);
    }

    [Fact]
    public void ExerciseTimeCostSeconds_UsesPlannedDurationForTimedExercises()
    {
        var cost = TimeBudgetCalculator.ExerciseTimeCostSeconds(sets: 3, reps: 0, plannedDurationSeconds: 45, restSeconds: 30);
        cost.Should().Be(45 + 3 * 45 + 2 * 30);
    }

    [Fact]
    public void EstimateQuestDurationSeconds_IncludesWarmupAndCooldown()
    {
        var total = TimeBudgetCalculator.EstimateQuestDurationSeconds([100, 200], warmupSeconds: 300, cooldownSeconds: 120);
        total.Should().Be(300 + 100 + 200 + 120);
    }

    [Fact]
    public void Resolve_NeverExceedsAvailableTime()
    {
        var items = Enumerable.Range(0, 8)
            .Select(i => new TimeBudgetItem($"ex{i}", Sets: 4, Reps: 12, PlannedDurationSeconds: null, RestSeconds: 90, PriorityScore: 1.0 - i * 0.01))
            .ToList();
        var request = new TimeBudgetRequest(items, ExtraCandidates: [], EffectiveExperienceLevel: "intermediate", Goal: "gain_muscle", AvailableMinutesPerWorkout: 20);

        var result = TimeBudgetCalculator.Resolve(request);

        result.EstimatedDurationSeconds.Should().BeLessThanOrEqualTo(result.TimeBudgetSeconds);
    }

    [Fact]
    public void Resolve_StrengthGoalReducesExercisesAndSetsBeforeTouchingRest()
    {
        var items = Enumerable.Range(0, 5)
            .Select(i => new TimeBudgetItem($"ex{i}", Sets: 4, Reps: 8, PlannedDurationSeconds: null, RestSeconds: 150, PriorityScore: 1.0 - i * 0.1))
            .ToList();
        var request = new TimeBudgetRequest(items, ExtraCandidates: [], EffectiveExperienceLevel: "advanced", Goal: "gain_strength", AvailableMinutesPerWorkout: 20);

        var result = TimeBudgetCalculator.Resolve(request);

        result.TimeAdjustmentApplied.Should().BeOneOf("reduced_exercises", "reduced_sets", "micro_quest");
        result.Items.Should().OnlyContain(i => i.RestSeconds == 150); // descanso do objetivo preservado (RN-004)
    }

    [Fact]
    public void Resolve_ConditioningGoalAppliesDensityWhenStillOverBudgetAfterVolumeCuts()
    {
        var items = new List<TimeBudgetItem> { new("ex0", Sets: 1, Reps: 12, PlannedDurationSeconds: null, RestSeconds: 300, PriorityScore: 1.0) };
        var request = new TimeBudgetRequest(items, ExtraCandidates: [], EffectiveExperienceLevel: "intermediate", Goal: "conditioning", AvailableMinutesPerWorkout: 16, MinExerciseCount: 1);

        var result = TimeBudgetCalculator.Resolve(request);

        result.DensityApplied.Should().BeTrue();
        result.Items.Single().RestSeconds.Should().BeLessThan(300);
    }

    [Fact]
    public void Resolve_ShortTimeUsesMicroQuestFormat()
    {
        var items = new List<TimeBudgetItem> { new("ex0", Sets: 3, Reps: 12, PlannedDurationSeconds: null, RestSeconds: 60, PriorityScore: 1.0) };
        var request = new TimeBudgetRequest(items, ExtraCandidates: [], EffectiveExperienceLevel: "beginner", Goal: "gain_muscle", AvailableMinutesPerWorkout: 10);

        var result = TimeBudgetCalculator.Resolve(request);

        result.IsMicroQuest.Should().BeTrue();
        result.WarmupSeconds.Should().Be(WorkoutTimeModel.MicroQuestWarmupSeconds);
        result.CooldownSeconds.Should().Be(0);
    }

    [Fact]
    public void Resolve_AddsSetsThenExtraExerciseWhenBelowMinUtilization()
    {
        var items = new List<TimeBudgetItem> { new("ex0", Sets: 2, Reps: 10, PlannedDurationSeconds: null, RestSeconds: 30, PriorityScore: 1.0) };
        var extra = new List<TimeBudgetItem> { new("ex1", Sets: 2, Reps: 10, PlannedDurationSeconds: null, RestSeconds: 30, PriorityScore: 0.9) };
        var request = new TimeBudgetRequest(items, extra, EffectiveExperienceLevel: "intermediate", Goal: "gain_muscle", AvailableMinutesPerWorkout: 30);

        var result = TimeBudgetCalculator.Resolve(request);

        result.TimeAdjustmentApplied.Should().Be("added_volume");
        result.Utilization.Should().BeGreaterThanOrEqualTo((double)WorkoutTimeModel.MinUtilization - 0.05);
    }

    [Fact]
    public void Resolve_IsDeterministic()
    {
        var items = new List<TimeBudgetItem> { new("ex0", Sets: 4, Reps: 10, PlannedDurationSeconds: null, RestSeconds: 90, PriorityScore: 1.0) };
        var request = new TimeBudgetRequest(items, ExtraCandidates: [], EffectiveExperienceLevel: "intermediate", Goal: "gain_muscle", AvailableMinutesPerWorkout: 12);

        var first = TimeBudgetCalculator.Resolve(request);
        var second = TimeBudgetCalculator.Resolve(request);

        first.Should().BeEquivalentTo(second);
    }
}
```

- [ ] **Step 2:** Rodar `dotnet test backend/tests/Awaken.UnitTests --filter FullyQualifiedName~TimeBudgetCalculatorTests` — deve falhar (implementação ainda não existe / classe vazia).
- [ ] **Step 3:** Colar a implementação de `TimeBudgetCalculator.cs` mostrada acima (substitui o arquivo criado na Task 1).
- [ ] **Step 4:** Rodar o mesmo filtro — todos os 9 testes devem passar. Ajustar constantes/ladder se algum caso de borda não bater (ex.: `Resolve_ConditioningGoalAppliesDensity...` pode precisar de `MinExerciseCount: 1` explícito para não cair em `reduced_exercises` antes de chegar em `density` — o teste já usa uma lista de 1 item, então `reduced_exercises`/`reduced_sets` não têm mais o que cortar e o fluxo cai direto em `density`).

---

## Task 3 — Integrar `TimeBudgetCalculator` em `WorkoutGeneratorService` + `Quest` + contratos

**Files:**
- Modify: `backend/src/Awaken.Application/Common/Interfaces/IWorkoutGeneratorService.cs`
- Modify: `backend/src/Awaken.Infrastructure/Services/WorkoutGeneratorService.cs`
- Modify: `backend/src/Awaken.Domain/Entities/Quests/Quest.cs`
- Modify: `backend/src/Awaken.Application/Quests/Commands/GenerateDailyQuest/GenerateDailyQuestCommandHandler.cs`
- Modify: `backend/src/Awaken.Application/Quests/Common/QuestResponseMapper.cs`
- Modify: `backend/src/Awaken.Contracts/Quests/QuestResponse.cs`
- Modify: `backend/src/Awaken.Contracts/Quests/QuestPreviewResponse.cs`
- Modify: `backend/src/Awaken.Infrastructure/Persistence/Configurations/QuestConfiguration.cs`

### 3.1 — `WorkoutGenerationResult` ganha campos de tempo (trailing, com default — não quebra chamadores)

Em `IWorkoutGeneratorService.cs`, adicionar ao final do record:
```csharp
public record WorkoutGenerationResult(
    string WorkoutJson,
    bool IsPersonalized,
    string GenerationMethod,
    string AppliedFiltersJson,
    string? ResolvedProgramKey = null,
    string? ResolvedDayKey = null,
    int? ResolvedDayIndex = null,
    string? SplitMapVersion = null,
    string? DailyWorkoutBlueprintJson = null,
    int? EstimatedDurationSeconds = null,
    int? TimeBudgetSeconds = null,
    string? TimeAdjustmentApplied = null,
    string? WorkoutTimeModelVersion = null);
```

### 3.2 — `Quest.cs`: novos campos + método de atribuição

Adicionar após `DailyWorkoutBlueprintJson`:
```csharp
    // US-242: orçamento de tempo determinístico usado para compor esta quest (RN-007).
    public int? EstimatedDurationSeconds { get; private set; }
    public int? TimeBudgetSeconds { get; private set; }
    public string? TimeAdjustmentApplied { get; private set; }
    public string? WorkoutTimeModelVersion { get; private set; }
```
E um novo método público:
```csharp
    /// US-242: registra o orçamento de tempo aplicado a esta geração, para auditoria (RN-007).
    public void AssignTimeBudget(
        int estimatedDurationSeconds, int timeBudgetSeconds, string timeAdjustmentApplied,
        string workoutTimeModelVersion, DateTime utcNow)
    {
        EstimatedDurationSeconds = estimatedDurationSeconds;
        TimeBudgetSeconds = timeBudgetSeconds;
        TimeAdjustmentApplied = timeAdjustmentApplied;
        WorkoutTimeModelVersion = workoutTimeModelVersion;
        UpdatedAtUtc = utcNow;
    }
```

### 3.3 — `WorkoutGeneratorService.GenerateWorkoutJsonAsync`: passo de ajuste depois da seleção/prescrição

Depois do bloco atual que computa `selectedExercises`/`prescription`/`SetsFor`/`TargetRpeFor` (linhas ~134-172 hoje) e **antes** de montar `catalogWorkout`, inserir:

```csharp
        // US-242: orçamento de tempo determinístico — roda depois da seleção/prescrição reais
        // (heurísticas de ExerciseSafetyFilter/ExerciseScoringEngine continuam como pré-filtro
        // aproximado interno; TimeBudgetCalculator é a fonte de verdade exposta/persistida).
        var selectedItems = selectedScores.Select(s => new TimeBudgetItem(
            ExerciseId: s.Exercise.ProviderExerciseId,
            Sets: SetsFor(s.Exercise),
            Reps: prescription.RepsMax ?? prescription.RepsMin,
            PlannedDurationSeconds: null,
            RestSeconds: prescription.RestSeconds,
            PriorityScore: s.StaticScore)).ToList();

        var extraCandidateItems = scored
            .Where(s => !selectedScores.Contains(s))
            .Select(s => new TimeBudgetItem(
                s.Exercise.ProviderExerciseId, prescription.Sets, prescription.RepsMax ?? prescription.RepsMin,
                null, prescription.RestSeconds, s.StaticScore))
            .ToList();

        var timeBudgetResolution = TimeBudgetCalculator.Resolve(new TimeBudgetRequest(
            SelectedItems: selectedItems,
            ExtraCandidates: extraCandidateItems,
            EffectiveExperienceLevel: profile.EffectiveExperienceLevel,
            Goal: profile.Goal,
            AvailableMinutesPerWorkout: profile.AvailableMinutesPerWorkout));

        var timeBudgetByExerciseId = timeBudgetResolution.Items.ToDictionary(i => i.ExerciseId);
        var finalSelectedExercises = selectedExercises
            .Where(e => timeBudgetByExerciseId.ContainsKey(e.ProviderExerciseId))
            .ToList();
        // extraCandidateItems que o ladder adicionou (added_volume) não estão em selectedExercises —
        // resolvê-los de volta ao ExerciseCatalog original via `scored` (candidatos já carregam a entidade).
        var byProviderId = scored.ToDictionary(s => s.Exercise.ProviderExerciseId, s => s.Exercise);
        foreach (var item in timeBudgetResolution.Items)
        {
            if (!finalSelectedExercises.Any(e => e.ProviderExerciseId == item.ExerciseId)
                && byProviderId.TryGetValue(item.ExerciseId, out var addedExercise))
                finalSelectedExercises.Add(addedExercise);
        }
        selectedExercises = finalSelectedExercises;

        int SetsForFinal(ExerciseCatalog exercise) =>
            timeBudgetByExerciseId.TryGetValue(exercise.ProviderExerciseId, out var item) ? item.Sets : SetsFor(exercise);
        int RestSecondsForFinal(ExerciseCatalog exercise) =>
            timeBudgetByExerciseId.TryGetValue(exercise.ProviderExerciseId, out var item) ? item.RestSeconds : prescription.RestSeconds;
```

Ajustar o `catalogWorkout` (o `sets`/`restSeconds` de cada exercício passam a usar `SetsForFinal`/`RestSecondsForFinal` em vez de `SetsFor`/`prescription.RestSeconds`), e adicionar ao objeto anônimo raiz:
```csharp
            estimatedDurationSeconds = timeBudgetResolution.EstimatedDurationSeconds,
            timeBudgetSeconds = timeBudgetResolution.TimeBudgetSeconds,
            timeAdjustmentApplied = timeBudgetResolution.TimeAdjustmentApplied,
            isMicroQuest = timeBudgetResolution.IsMicroQuest,
            workoutTimeModelVersion = WorkoutTimeModel.Version,
```

E no `return new WorkoutGenerationResult(...)` do caminho personalizado, preencher os 4 novos campos com os valores de `timeBudgetResolution`/`WorkoutTimeModel.Version`.

Nos dois caminhos de fallback (`BuildFallbackWorkoutJson`), adicionar os mesmos campos com valores conservadores: `estimatedDurationSeconds = availableMinutesPerWorkout * 60`, `timeBudgetSeconds` igual, `timeAdjustmentApplied = "none"`, `isMicroQuest = availableMinutesPerWorkout <= WorkoutTimeModel.MicroQuestThresholdMinutes`, `workoutTimeModelVersion = WorkoutTimeModel.Version` — e propagar no `WorkoutGenerationResult` dos dois `return` de fallback.

### 3.4 — `GenerateDailyQuestCommandHandler.cs`: persistir os novos campos

Depois do bloco `if (workoutResult.DailyWorkoutBlueprintJson is not null) { ... }`, adicionar:
```csharp
        if (workoutResult.EstimatedDurationSeconds is not null
            && workoutResult.TimeBudgetSeconds is not null
            && workoutResult.TimeAdjustmentApplied is not null
            && workoutResult.WorkoutTimeModelVersion is not null)
        {
            quest.AssignTimeBudget(
                workoutResult.EstimatedDurationSeconds.Value,
                workoutResult.TimeBudgetSeconds.Value,
                workoutResult.TimeAdjustmentApplied,
                workoutResult.WorkoutTimeModelVersion,
                utcNow);
        }
```

### 3.5 — Expor no `WorkoutDto`/`QuestPreviewResponse` pro Flutter

Em `Awaken.Contracts/Quests/QuestResponse.cs`, adicionar ao `WorkoutDto`:
```csharp
    int? EstimatedDurationSeconds = null,
    string? TimeAdjustmentApplied = null,
    bool IsMicroQuest = false,
```
Em `QuestResponseMapper.RawWorkout`, adicionar:
```csharp
        public int? EstimatedDurationSeconds { get; set; }
        public string? TimeAdjustmentApplied { get; set; }
        public bool? IsMicroQuest { get; set; }
```
E no `ParseWorkout`, propagar pro `WorkoutDto` (`EstimatedDurationSeconds: raw.EstimatedDurationSeconds`, `TimeAdjustmentApplied: raw.TimeAdjustmentApplied`, `IsMicroQuest: raw.IsMicroQuest ?? false`).

- [ ] **Step 1:** Aplicar as mudanças 3.1-3.5.
- [ ] **Step 2:** Rodar `dotnet build`. Corrigir qualquer erro de compilação (ex.: `TimeBudgetItem`/`TimeBudgetRequest` precisam de `using Awaken.Domain.Services.Quests;` em `WorkoutGeneratorService.cs`, já presente).
- [ ] **Step 3:** Rodar `dotnet test backend/tests/Awaken.UnitTests --filter FullyQualifiedName~WorkoutGeneratorServiceTests`. Testes existentes que fazem `JsonDocument.Parse(result.WorkoutJson)` e comparam contagem de campos ou de exercícios podem quebrar se o ladder de tempo remover/adicionar exercícios em cenários com `AvailableMinutesPerWorkout` pequeno/grande nos fixtures. Ajustar fixtures existentes para `AvailableMinutesPerWorkout` realista (ex.: 45-60 min) quando o teste não for especificamente sobre orçamento de tempo, e criar 2 novos testes:
  - `GenerateWorkoutJsonAsync_IncludesTimeBudgetFieldsInWorkoutJson` — dado um perfil com `availableMinutesPerWorkout=30`, o JSON resultante contém `estimatedDurationSeconds`, `timeBudgetSeconds`, `timeAdjustmentApplied`, `workoutTimeModelVersion`.
  - `GenerateWorkoutJsonAsync_NeverExceedsAvailableMinutes` — `estimatedDurationSeconds <= availableMinutesPerWorkout * 60`.
- [ ] **Step 4:** Rodar a suíte completa (`dotnet test backend/tests/Awaken.UnitTests`), corrigir qualquer outro teste quebrado (ex.: `GenerateDailyQuestCommandHandlerTests`, `QuestResponseMapperTests` se existir, testes de integração de `QuestGenerationEndpointTests`).

---

## Task 4 — Migration: campos de tempo em `Quest`

**Files:**
- Create: `backend/src/Awaken.Infrastructure/Persistence/Migrations/20260706060000_AddTimeBudgetFieldsToQuests.cs` (+ `.Designer.cs`, gerado)
- Modify: `backend/src/Awaken.Infrastructure/Persistence/Configurations/QuestConfiguration.cs`

Em `QuestConfiguration.cs`, adicionar (mesmo padrão das colunas `DailyWorkoutBlueprintJson`/`ResolvedDayKey` já existentes — `.IsRequired(false)` para todos, sem índice):
```csharp
        builder.Property(q => q.EstimatedDurationSeconds);
        builder.Property(q => q.TimeBudgetSeconds);
        builder.Property(q => q.TimeAdjustmentApplied).HasMaxLength(32);
        builder.Property(q => q.WorkoutTimeModelVersion).HasMaxLength(16);
```

- [ ] **Step 1:** Aplicar a config acima.
- [ ] **Step 2:** Gerar a migration:
```bash
dotnet ef migrations add AddTimeBudgetFieldsToQuests -p backend/src/Awaken.Infrastructure -s backend/src/Awaken.Api
```
- [ ] **Step 3:** Conferir o arquivo gerado (`Up`/`Down` só devem ter `AddColumn`/`DropColumn` para as 4 colunas novas, nullable). Renomear o timestamp se colidir com alguma migration já existente no diretório.
- [ ] **Step 4:** Rodar `dotnet build` (compila o `*.Designer.cs`/`AwakenDbContextModelSnapshot.cs` atualizado). Não aplicar `dotnet ef database update` ainda — deixar para a Task 13 (verificação final), já que a Task 8 desta plan também vai adicionar migrations.

---

## Task 5 — `QuestLog`/`CompleteQuest`: capturar "como você se sentiu" (sinal real da US-241)

**Files:**
- Modify: `backend/src/Awaken.Domain/Entities/Quests/QuestLog.cs`
- Modify: `backend/src/Awaken.Application/Quests/Commands/CompleteQuest/CompleteQuestCommand.cs`
- Modify: `backend/src/Awaken.Application/Quests/Commands/CompleteQuest/CompleteQuestCommandValidator.cs`
- Modify: `backend/src/Awaken.Application/Quests/Commands/CompleteQuest/CompleteQuestCommandHandler.cs`
- Modify: `backend/src/Awaken.Infrastructure/Persistence/Configurations/QuestLogConfiguration.cs`
- Test: `backend/tests/Awaken.UnitTests/Quests/CompleteQuestCommandHandlerTests.cs` (estender)
- Test: `backend/tests/Awaken.UnitTests/Quests/CompleteQuestCommandValidatorTests.cs` (criar se não existir)

### 5.1 — `PerceivedFeeling` como constante fechada (mesmo padrão de `MuscleGroups`/`TrainingProgramKeys`)

Create: `backend/src/Awaken.Domain/Entities/Quests/PerceivedFeelings.cs`
```csharp
namespace Awaken.Domain.Entities.Quests;

/// US-241 §6.2: proxy de 3 bandas para "RPE médio × RPE-alvo" — capturado como
/// uma pergunta simples ao usuário na conclusão da quest (decisão de escopo:
/// reps/sets sempre consideradas "atingidas conforme prescrito").
public static class PerceivedFeelings
{
    public const string TooEasy = "too_easy";
    public const string JustRight = "just_right";
    public const string TooHard = "too_hard";

    public static readonly IReadOnlyCollection<string> All = [TooEasy, JustRight, TooHard];

    public static bool IsValid(string? value) => value is null || All.Contains(value, StringComparer.Ordinal);
}
```

### 5.2 — `QuestLog.cs`: novo campo

Adicionar `public string? PerceivedFeeling { get; private set; }` e um parâmetro opcional `string? perceivedFeeling = null` no final da assinatura de `Create(...)`, atribuído em `PerceivedFeeling = perceivedFeeling` dentro do objeto retornado.

### 5.3 — `CompleteQuestCommand`/Validator/Handler

```csharp
public record CompleteQuestCommand(Guid QuestId, string? PerceivedFeeling = null) : IRequest<CompleteQuestResponse>;
```
Validator:
```csharp
        RuleFor(x => x.PerceivedFeeling)
            .Must(Awaken.Domain.Entities.Quests.PerceivedFeelings.IsValid)
            .WithMessage("PerceivedFeeling deve ser too_easy, just_right, too_hard ou nulo.");
```
No handler, no `QuestLog.Create(...)`, passar `perceivedFeeling: request.PerceivedFeeling` como último argumento (nomeado, não posicional, pra não depender da ordem).

### 5.4 — `QuestLogConfiguration.cs`

Adicionar `builder.Property(q => q.PerceivedFeeling).HasMaxLength(16);`

- [ ] **Step 1:** Escrever o teste (falha esperada antes da implementação):
```csharp
[Fact]
public async Task Handle_PersistsPerceivedFeelingOnQuestLog()
{
    // Arrange: quest in_progress existente (reaproveitar builder de fixture já usado nos outros testes desta classe).
    var command = new CompleteQuestCommand(QuestId: _quest.Id, PerceivedFeeling: "too_easy");

    await CreateHandler().Handle(command, CancellationToken.None);

    _questLogRepository.Verify(r => r.AddAsync(
        It.Is<QuestLog>(log => log.PerceivedFeeling == "too_easy"), It.IsAny<CancellationToken>()), Times.Once);
}
```
(Adaptar ao setup real de fixture já existente em `CompleteQuestCommandHandlerTests.cs` — reaproveitar o helper que monta a quest `in_progress`.)
- [ ] **Step 2:** Rodar o teste, confirmar falha (`CompleteQuestCommand` ainda não tem o parâmetro).
- [ ] **Step 3:** Implementar 5.1-5.4.
- [ ] **Step 4:** Rodar o teste — deve passar. Rodar a suíte inteira de `CompleteQuestCommandHandlerTests` para garantir que os testes existentes (que chamam `new CompleteQuestCommand(questId)` sem o segundo argumento) continuam passando (default `null` cobre isso).
- [ ] **Step 5:** Criar migration:
```bash
dotnet ef migrations add AddPerceivedFeelingToQuestLogs -p backend/src/Awaken.Infrastructure -s backend/src/Awaken.Api
```

---

## Task 6 — `WeeklyProgressionState` (entidade + repositório + config)

**Files:**
- Create: `backend/src/Awaken.Domain/Entities/Progression/WeeklyProgressionState.cs`
- Create: `backend/src/Awaken.Domain/Repositories/IWeeklyProgressionStateRepository.cs`
- Create: `backend/src/Awaken.Infrastructure/Persistence/Repositories/WeeklyProgressionStateRepository.cs`
- Create: `backend/src/Awaken.Infrastructure/Persistence/Configurations/WeeklyProgressionStateConfiguration.cs`
- Modify: `backend/src/Awaken.Infrastructure/Persistence/AwakenDbContext.cs` (+`DbSet<WeeklyProgressionState> WeeklyProgressionStates`)
- Modify: `backend/src/Awaken.Infrastructure/DependencyInjection.cs` (+registro do repositório)
- Test: `backend/tests/Awaken.UnitTests/Progression/WeeklyProgressionStateTests.cs`

```csharp
namespace Awaken.Domain.Entities.Progression;

/// US-241 §14: estado de progressão semanal por usuário. `VolumeSetsDelta` é o
/// número de séries acrescidas sobre o baseline da prescrição (US-153), capado
/// pelo teto recuperável do blueprint (US-239/US-240) no momento de aplicar,
/// não aqui (este estado não conhece o blueprint).
public class WeeklyProgressionState : BaseEntity
{
    public const int MaxConsecutiveWeeksBeforeDeload = 5; // ~4-6 semanas (RN-005)
    public const int MaxConsecutiveHardWeeksBeforeDeload = 2;
    public const int MaxVolumeSetsDelta = 4;

    public Guid UserId { get; private set; }
    public DateOnly WeekAnchorDate { get; private set; }
    public int MesocycleWeekIndex { get; private set; } = 1;
    public string? ProfileSnapshotHash { get; private set; }
    public int ConsecutiveEasyWeeks { get; private set; }
    public int ConsecutiveHardWeeks { get; private set; }
    public bool DeloadDue { get; private set; }
    public string LastDecision { get; private set; } = "hold";
    public string? LastAxis { get; private set; }
    public int VolumeSetsDelta { get; private set; }
    public int RpeDelta { get; private set; }
    public int RestSecondsDelta { get; private set; }

    private WeeklyProgressionState() { }

    public static WeeklyProgressionState CreateInitial(Guid userId, DateOnly weekAnchorDate, string? profileSnapshotHash, DateTime utcNow) =>
        new()
        {
            UserId = userId,
            WeekAnchorDate = weekAnchorDate,
            ProfileSnapshotHash = profileSnapshotHash,
            UpdatedAtUtc = utcNow,
        };

    /// US-241 RN-001/RN-006: aplica o resultado da reavaliação semanal (nova
    /// semana OU mudança de perfil), avançando o mesociclo quando de fato é
    /// uma nova semana (não em recalibração pura por mudança de perfil).
    public void ApplyWeeklyDecision(
        DateOnly newWeekAnchorDate,
        bool isNewWeek,
        string decision,
        string? axis,
        int volumeSetsDelta,
        int rpeDelta,
        int restSecondsDelta,
        int consecutiveEasyWeeks,
        int consecutiveHardWeeks,
        bool deloadApplied,
        string? profileSnapshotHash,
        DateTime utcNow)
    {
        WeekAnchorDate = newWeekAnchorDate;
        if (isNewWeek)
            MesocycleWeekIndex = deloadApplied ? 1 : MesocycleWeekIndex + 1;

        LastDecision = decision;
        LastAxis = axis;
        VolumeSetsDelta = volumeSetsDelta;
        RpeDelta = rpeDelta;
        RestSecondsDelta = restSecondsDelta;
        ConsecutiveEasyWeeks = consecutiveEasyWeeks;
        ConsecutiveHardWeeks = consecutiveHardWeeks;
        DeloadDue = deloadApplied;
        ProfileSnapshotHash = profileSnapshotHash;
        UpdatedAtUtc = utcNow;
    }
}
```

`IWeeklyProgressionStateRepository`:
```csharp
namespace Awaken.Domain.Repositories;

public interface IWeeklyProgressionStateRepository : IRepository<Awaken.Domain.Entities.Progression.WeeklyProgressionState>
{
    Task<Awaken.Domain.Entities.Progression.WeeklyProgressionState?> GetByUserIdAsync(Guid userId, CancellationToken cancellationToken = default);
}
```

`WeeklyProgressionStateRepository` (mesmo padrão de `MuscleRecoveryStateRepository`):
```csharp
using Awaken.Domain.Entities.Progression;
using Awaken.Domain.Repositories;
using Awaken.Infrastructure.Persistence;
using Microsoft.EntityFrameworkCore;

namespace Awaken.Infrastructure.Persistence.Repositories;

public class WeeklyProgressionStateRepository(AwakenDbContext context)
    : Repository<WeeklyProgressionState>(context), IWeeklyProgressionStateRepository
{
    public Task<WeeklyProgressionState?> GetByUserIdAsync(Guid userId, CancellationToken cancellationToken = default) =>
        context.WeeklyProgressionStates.FirstOrDefaultAsync(s => s.UserId == userId, cancellationToken);
}
```
(Confirmar o nome exato da classe base genérica `Repository<T>` e o namespace do `DbContext` abrindo `MuscleRecoveryStateRepository.cs` como referência antes de escrever — copiar a assinatura do construtor exatamente.)

`WeeklyProgressionStateConfiguration` (mesmo padrão de `MuscleRecoveryStateConfiguration.cs`): tabela `weekly_progression_states`, `HasIndex(s => s.UserId).IsUnique()`, `ProfileSnapshotHash`/`LastDecision`/`LastAxis` com `HasMaxLength` (64/16/32 respectivamente).

- [ ] **Step 1:** Escrever teste de domínio puro:
```csharp
[Fact]
public void ApplyWeeklyDecision_AdvancesMesocycleOnlyOnNewWeek()
{
    var state = WeeklyProgressionState.CreateInitial(Guid.NewGuid(), new DateOnly(2026, 7, 6), null, DateTime.UtcNow);
    state.ApplyWeeklyDecision(new DateOnly(2026, 7, 6), isNewWeek: false, "hold", null, 0, 0, 0, 0, 0, false, null, DateTime.UtcNow);
    state.MesocycleWeekIndex.Should().Be(1);

    state.ApplyWeeklyDecision(new DateOnly(2026, 7, 13), isNewWeek: true, "progress", "add_set", 1, 0, 0, 1, 0, false, null, DateTime.UtcNow);
    state.MesocycleWeekIndex.Should().Be(2);
}

[Fact]
public void ApplyWeeklyDecision_ResetsMesocycleToOneWhenDeloadApplied()
{
    var state = WeeklyProgressionState.CreateInitial(Guid.NewGuid(), new DateOnly(2026, 7, 6), null, DateTime.UtcNow);
    for (var i = 0; i < 5; i++)
        state.ApplyWeeklyDecision(new DateOnly(2026, 7, 6).AddDays(7 * (i + 1)), true, "progress", "add_set", i, 0, 0, i + 1, 0, false, null, DateTime.UtcNow);

    state.ApplyWeeklyDecision(new DateOnly(2026, 8, 10), isNewWeek: true, "deload", null, 0, -1, 0, 0, 0, deloadApplied: true, null, DateTime.UtcNow);
    state.MesocycleWeekIndex.Should().Be(1);
    state.DeloadDue.Should().BeTrue();
}
```
- [ ] **Step 2:** Rodar, confirmar falha (classe não existe).
- [ ] **Step 3:** Implementar a entidade + repositório + config + DbSet + DI.
- [ ] **Step 4:** Rodar `dotnet build` + os 2 testes acima — devem passar.

---

## Task 7 — `WeeklyProgressionDecisionEngine` (domínio puro — coração da US-241)

**Files:**
- Create: `backend/src/Awaken.Domain/Services/Progression/WeeklyProgressionDecisionEngine.cs`
- Test: `backend/tests/Awaken.UnitTests/Progression/WeeklyProgressionDecisionEngineTests.cs`

```csharp
using Awaken.Domain.Entities.Progression;
using Awaken.Domain.Entities.Quests;

namespace Awaken.Domain.Services.Progression;

/// <summary>
/// US-241 §6.2/6.3/6.6: decide progredir/manter/regredir/deload a partir do
/// sentimento predominante da semana (proxy de RPE, ver Task 5) e aplica o
/// viés de rank/atributos (RN-007), sempre avançando no máximo 1 eixo por vez
/// (RN-003). Determinístico e puro — não conhece banco nem HTTP.
/// </summary>
public static class WeeklyProgressionDecisionEngine
{
    // RN-003: ordem fixa do único eixo que pode avançar por vez.
    private static readonly string[] AxisCycle = ["reps", "add_set", "harder_variant", "less_rest", "higher_rpe"];
    private const int HighAttributeThreshold = 6; // de 0-10 (US-130) — "alto" para fins de viés (US-241 §6.6).

    public static WeeklyProgressionDecision Decide(WeeklyProgressionDecisionRequest request)
    {
        var dominantFeeling = DominantFeeling(request.RecentFeelings);

        var deloadTriggered =
            request.MesocycleWeekIndex >= WeeklyProgressionState.MaxConsecutiveWeeksBeforeDeload ||
            request.ConsecutiveHardWeeks >= WeeklyProgressionState.MaxConsecutiveHardWeeksBeforeDeload;

        if (deloadTriggered)
        {
            return new WeeklyProgressionDecision(
                Decision: "deload",
                Axis: null,
                VolumeSetsDelta: (int)Math.Round(request.CurrentVolumeSetsDelta * 0.5, MidpointRounding.AwayFromZero),
                RpeDelta: -1,
                RestSecondsDelta: 0,
                ConsecutiveEasyWeeks: 0,
                ConsecutiveHardWeeks: 0,
                DeloadWeek: true,
                AttributeBias: BuildAttributeBias(request.Attributes));
        }

        if (dominantFeeling is null)
        {
            // RN (10.4): dados insuficientes -> mantém o patamar, não progride às cegas.
            return new WeeklyProgressionDecision(
                "hold", null, request.CurrentVolumeSetsDelta, 0, 0,
                0, request.ConsecutiveHardWeeks, false, BuildAttributeBias(request.Attributes));
        }

        if (dominantFeeling == PerceivedFeelings.TooHard)
        {
            var regressedVolume = Math.Max(0, request.CurrentVolumeSetsDelta - 1);
            return new WeeklyProgressionDecision(
                "regress", null, regressedVolume, 0, 0,
                0, request.ConsecutiveHardWeeks + 1, false, BuildAttributeBias(request.Attributes));
        }

        if (dominantFeeling == PerceivedFeelings.JustRight)
        {
            return new WeeklyProgressionDecision(
                "hold", "reps", request.CurrentVolumeSetsDelta, 0, 0,
                request.ConsecutiveEasyWeeks, 0, false, BuildAttributeBias(request.Attributes));
        }

        // too_easy -> progredir em exatamente 1 eixo (RN-003), avançando o ciclo a partir do último eixo usado.
        var maxVolume = WeeklyProgressionState.MaxVolumeSetsDelta + (HasHighAttribute(request.Attributes, "vitality") ? 1 : 0);
        var axis = NextAxis(request.LastAxis, request.CurrentVolumeSetsDelta, maxVolume);

        var volumeDelta = axis == "add_set"
            ? Math.Min(maxVolume, request.CurrentVolumeSetsDelta + 1)
            : request.CurrentVolumeSetsDelta;
        var rpeDelta = axis == "higher_rpe" ? 1 : 0;
        var restDelta = axis == "less_rest" ? -10 : 0;

        return new WeeklyProgressionDecision(
            "progress", axis, volumeDelta, rpeDelta, restDelta,
            request.ConsecutiveEasyWeeks + 1, 0, false, BuildAttributeBias(request.Attributes));
    }

    private static string? DominantFeeling(IReadOnlyList<string> recentFeelings)
    {
        if (recentFeelings.Count == 0) return null;

        return recentFeelings
            .GroupBy(f => f, StringComparer.Ordinal)
            .OrderByDescending(g => g.Count())
            .ThenBy(g => Array.IndexOf(PerceivedFeelings.All.ToArray(), g.Key)) // desempate determinístico
            .First().Key;
    }

    private static string NextAxis(string? lastAxis, int currentVolumeSetsDelta, int maxVolume)
    {
        var startIndex = lastAxis is null ? -1 : Array.IndexOf(AxisCycle, lastAxis);
        for (var offset = 1; offset <= AxisCycle.Length; offset++)
        {
            var candidate = AxisCycle[(startIndex + offset) % AxisCycle.Length];
            // RN-004: não insiste em "add_set" além do teto recuperável -> pula para o próximo eixo.
            if (candidate == "add_set" && currentVolumeSetsDelta >= maxVolume) continue;
            return candidate;
        }
        return AxisCycle[0];
    }

    private static bool HasHighAttribute(IReadOnlyDictionary<string, int> attributes, string attribute) =>
        attributes.TryGetValue(attribute, out var value) && value >= HighAttributeThreshold;

    /// US-241 §6.6: vetor de viés por atributo, dentro dos limites de segurança/recuperação
    /// (aplicado pelo chamador sobre a prescrição — este método só sinaliza a direção).
    private static IReadOnlyDictionary<string, int> BuildAttributeBias(IReadOnlyDictionary<string, int> attributes)
    {
        var bias = new Dictionary<string, int>(StringComparer.Ordinal);
        if (HasHighAttribute(attributes, "strength")) bias["strength"] = 1;   // reps- / variante mais pesada
        if (HasHighAttribute(attributes, "endurance")) bias["endurance"] = 1; // reps+ / descanso-
        if (HasHighAttribute(attributes, "vitality")) bias["vitality"] = 1;   // +1 teto de volume (já aplicado acima)
        return bias;
    }
}

public sealed record WeeklyProgressionDecisionRequest(
    IReadOnlyList<string> RecentFeelings,
    int ConsecutiveEasyWeeks,
    int ConsecutiveHardWeeks,
    int MesocycleWeekIndex,
    int CurrentVolumeSetsDelta,
    string Rank,
    IReadOnlyDictionary<string, int> Attributes);

public sealed record WeeklyProgressionDecision(
    string Decision,
    string? Axis,
    int VolumeSetsDelta,
    int RpeDelta,
    int RestSecondsDelta,
    int ConsecutiveEasyWeeks,
    int ConsecutiveHardWeeks,
    bool DeloadWeek,
    IReadOnlyDictionary<string, int> AttributeBias);
```

- [ ] **Step 1:** Escrever os testes (cobrem CA-001 a CA-006 da US-241):
```csharp
using Awaken.Domain.Entities.Quests;
using Awaken.Domain.Services.Progression;
using FluentAssertions;
using Xunit;

namespace Awaken.UnitTests.Progression;

public class WeeklyProgressionDecisionEngineTests
{
    private static WeeklyProgressionDecisionRequest BaseRequest(
        IReadOnlyList<string> feelings, int mesocycleWeek = 2, int consecutiveHard = 0,
        int currentVolume = 0, string? lastAxis = null, IReadOnlyDictionary<string, int>? attributes = null) =>
        new(feelings, ConsecutiveEasyWeeks: 0, consecutiveHard, mesocycleWeek, currentVolume,
            Rank: "D", attributes ?? new Dictionary<string, int>());

    [Fact] // CA-001
    public void Decide_ProgressesExactlyOneAxisWhenTooEasy()
    {
        var request = BaseRequest([PerceivedFeelings.TooEasy, PerceivedFeelings.TooEasy]);
        var decision = WeeklyProgressionDecisionEngine.Decide(request);

        decision.Decision.Should().Be("progress");
        decision.Axis.Should().Be("reps"); // primeiro eixo do ciclo quando não há LastAxis
    }

    [Fact] // CA-002
    public void Decide_HoldsOrRegressesWithoutAddingOverloadWhenTooHard()
    {
        var request = BaseRequest([PerceivedFeelings.TooHard], currentVolume: 2);
        var decision = WeeklyProgressionDecisionEngine.Decide(request);

        decision.Decision.Should().Be("regress");
        decision.VolumeSetsDelta.Should().BeLessThanOrEqualTo(2);
        decision.RpeDelta.Should().BeLessThanOrEqualTo(0);
    }

    [Fact] // CA-003
    public void Decide_TriggersDeloadAfterFiveConsecutiveWeeks()
    {
        var request = BaseRequest([PerceivedFeelings.TooEasy], mesocycleWeek: 5, currentVolume: 4);
        var decision = WeeklyProgressionDecisionEngine.Decide(request);

        decision.Decision.Should().Be("deload");
        decision.DeloadWeek.Should().BeTrue();
        decision.VolumeSetsDelta.Should().BeLessThan(4);
    }

    [Fact] // CA-003 (gatilho alternativo)
    public void Decide_TriggersDeloadAfterTwoConsecutiveHardWeeks()
    {
        var request = BaseRequest([PerceivedFeelings.TooHard], consecutiveHard: 2);
        var decision = WeeklyProgressionDecisionEngine.Decide(request);

        decision.Decision.Should().Be("deload");
    }

    [Fact] // RN (10.4) dados insuficientes
    public void Decide_HoldsWhenNoFeelingDataAvailable()
    {
        var decision = WeeklyProgressionDecisionEngine.Decide(BaseRequest([]));

        decision.Decision.Should().Be("hold");
        decision.Axis.Should().BeNull();
    }

    [Fact] // RN-003: só 1 eixo por vez, avança o ciclo a partir do último usado
    public void Decide_AdvancesToNextAxisInCycleWhenProgressingRepeatedly()
    {
        var decision = WeeklyProgressionDecisionEngine.Decide(
            BaseRequest([PerceivedFeelings.TooEasy], lastAxis: "reps"));

        decision.Axis.Should().Be("add_set");
    }

    [Fact] // RN-004: teto de volume -> pula add_set e avança pro próximo eixo
    public void Decide_SkipsAddSetAxisWhenVolumeCapReached()
    {
        var decision = WeeklyProgressionDecisionEngine.Decide(
            BaseRequest([PerceivedFeelings.TooEasy], lastAxis: "reps", currentVolume: 4));

        decision.Axis.Should().Be("harder_variant");
        decision.VolumeSetsDelta.Should().Be(4); // não ultrapassa o teto
    }

    [Fact] // CA-005
    public void Decide_HighStrengthGetsAttributeBiasTowardHeavierVariants()
    {
        var withHighStrength = WeeklyProgressionDecisionEngine.Decide(
            BaseRequest([PerceivedFeelings.TooEasy], attributes: new Dictionary<string, int> { ["strength"] = 8 }));
        var withLowStrength = WeeklyProgressionDecisionEngine.Decide(
            BaseRequest([PerceivedFeelings.TooEasy], attributes: new Dictionary<string, int> { ["strength"] = 1 }));

        withHighStrength.AttributeBias.Should().ContainKey("strength");
        withLowStrength.AttributeBias.Should().NotContainKey("strength");
    }

    [Fact] // RN-011: regressão não deve carregar sinal de penalidade de XP (aqui só garantimos que a decisão em si é neutra quanto a XP - não há campo de XP no record)
    public void Decide_RegressDoesNotIncludeDeloadFlag()
    {
        var decision = WeeklyProgressionDecisionEngine.Decide(BaseRequest([PerceivedFeelings.TooHard]));
        decision.DeloadWeek.Should().BeFalse();
    }

    [Fact] // Determinismo
    public void Decide_IsDeterministic()
    {
        var request = BaseRequest([PerceivedFeelings.TooEasy, PerceivedFeelings.JustRight]);
        var first = WeeklyProgressionDecisionEngine.Decide(request);
        var second = WeeklyProgressionDecisionEngine.Decide(request);

        first.Should().BeEquivalentTo(second);
    }
}
```
- [ ] **Step 2:** Rodar, confirmar falha (classe não existe).
- [ ] **Step 3:** Implementar `WeeklyProgressionDecisionEngine.cs`.
- [ ] **Step 4:** Rodar os 10 testes — todos devem passar. Ajustar `NextAxis`/`DominantFeeling` se a ordem de desempate não bater com alguma expectativa.

---

## Task 8 — `IQuestLogRepository.GetCompletedSinceAsync` (dado de entrada real do reviewer)

**Files:**
- Modify: `backend/src/Awaken.Domain/Repositories/IQuestLogRepository.cs`
- Modify: `backend/src/Awaken.Infrastructure/Persistence/Repositories/QuestLogRepository.cs`
- Test: estender `backend/tests/Awaken.IntegrationTests` só se já houver suíte de integração pra `QuestLogRepository` — senão, cobrir via teste do `WeeklyProgressionReviewer` (Task 9, com mock).

```csharp
    /// US-241 §9.1: logs concluídos desde uma data (para extrair o sentimento
    /// predominante da semana). Ordenado do mais recente ao mais antigo.
    Task<IReadOnlyList<QuestLog>> GetCompletedSinceAsync(Guid userId, DateTime sinceUtc, CancellationToken cancellationToken = default);
```
Implementação (mesmo padrão de `GetRecentByUserIdAsync`):
```csharp
    public async Task<IReadOnlyList<QuestLog>> GetCompletedSinceAsync(Guid userId, DateTime sinceUtc, CancellationToken cancellationToken = default) =>
        await context.QuestLogs
            .Where(l => l.UserId == userId && l.CompletedAtUtc >= sinceUtc)
            .OrderByDescending(l => l.CompletedAtUtc)
            .ToListAsync(cancellationToken);
```

- [ ] **Step 1:** Aplicar as mudanças.
- [ ] **Step 2:** Rodar `dotnet build`. Sem teste dedicado aqui (coberto indiretamente pela Task 9 via mock da interface).

---

## Task 9 — `WeeklyProgressionReviewer` (orquestrador Application, análogo a `DailyWorkoutBlueprintBuilder`)

**Files:**
- Create: `backend/src/Awaken.Application/Progression/Common/WeeklyProgressionReviewer.cs`
- Test: `backend/tests/Awaken.UnitTests/Progression/WeeklyProgressionReviewerTests.cs`

```csharp
using System.Security.Cryptography;
using System.Text;
using Awaken.Application.Common.Interfaces;
using Awaken.Domain.Entities.Onboarding;
using Awaken.Domain.Entities.Progression;
using Awaken.Domain.Repositories;
using Awaken.Domain.Services.Progression;
using Microsoft.Extensions.Logging;

namespace Awaken.Application.Progression.Common;

/// US-241: reavalia semanalmente (ou por mudança de perfil, via comparação de
/// hash - ver §0 do plano) o estado do jogador e emite o `WeeklyProgressionPlan`
/// consumido pela geração (US-153/US-240). Fica em Application (orquestra
/// repositórios), mesmo precedente de `DailyWorkoutBlueprintBuilder`.
public class WeeklyProgressionReviewer(
    IWeeklyProgressionStateRepository stateRepository,
    IQuestLogRepository questLogRepository,
    IDateTimeService dateTimeService,
    ILogger<WeeklyProgressionReviewer> logger)
{
    private const int RecentFeelingsWindowCount = 7;

    public async Task<WeeklyProgressionPlan> ReviewAsync(
        Guid userId, UserProfile profile, HunterProgression? progression, CancellationToken ct = default)
    {
        var utcNow = dateTimeService.UtcNow;
        var currentWeekAnchor = WeekAnchorFor(DateOnly.FromDateTime(utcNow));
        var profileHash = ComputeProfileHash(profile);

        var state = await stateRepository.GetByUserIdAsync(userId, ct);
        var isFirstEvaluation = state is null;
        if (state is null)
        {
            state = WeeklyProgressionState.CreateInitial(userId, currentWeekAnchor, profileHash, utcNow);
            await stateRepository.AddAsync(state, ct);
        }

        var isNewWeek = state.WeekAnchorDate != currentWeekAnchor;
        var profileChanged = !isFirstEvaluation && state.ProfileSnapshotHash != profileHash;

        if (!isFirstEvaluation && !isNewWeek && !profileChanged)
        {
            // Mesma semana, sem mudança de perfil: devolve o plano já vigente (idempotente).
            return BuildPlan(state, currentWeekAnchor, recalibrated: false);
        }

        var recentLogs = await questLogRepository.GetCompletedSinceAsync(
            userId, utcNow.AddDays(-RecentFeelingsWindowCount), ct);
        var recentFeelings = recentLogs
            .Where(l => l.PerceivedFeeling is not null)
            .Select(l => l.PerceivedFeeling!)
            .ToList();

        var attributes = progression is null
            ? new Dictionary<string, int>()
            : new Dictionary<string, int>(StringComparer.Ordinal)
            {
                ["strength"] = progression.Strength,
                ["agility"] = progression.Agility,
                ["endurance"] = progression.Endurance,
                ["vitality"] = progression.Vitality,
                ["focus"] = progression.Focus,
                ["wisdom"] = progression.Wisdom,
            };

        var decision = WeeklyProgressionDecisionEngine.Decide(new WeeklyProgressionDecisionRequest(
            RecentFeelings: recentFeelings,
            ConsecutiveEasyWeeks: state.ConsecutiveEasyWeeks,
            ConsecutiveHardWeeks: state.ConsecutiveHardWeeks,
            MesocycleWeekIndex: state.MesocycleWeekIndex,
            CurrentVolumeSetsDelta: state.VolumeSetsDelta,
            Rank: progression?.Rank ?? "E",
            Attributes: attributes));

        state.ApplyWeeklyDecision(
            currentWeekAnchor,
            isNewWeek || isFirstEvaluation,
            decision.Decision,
            decision.Axis,
            decision.VolumeSetsDelta,
            decision.RpeDelta,
            decision.RestSecondsDelta,
            decision.ConsecutiveEasyWeeks,
            decision.ConsecutiveHardWeeks,
            decision.DeloadWeek,
            profileHash,
            utcNow);

        if (!isFirstEvaluation)
            stateRepository.Update(state);

        logger.LogInformation(
            "weekly_progression_reviewed userId={UserId} decision={Decision} axis={Axis} deloadWeek={DeloadWeek} mesocycleWeekIndex={MesocycleWeekIndex} recalibratedFromProfileChange={ProfileChanged}",
            userId, decision.Decision, decision.Axis, decision.DeloadWeek, state.MesocycleWeekIndex, profileChanged);

        return BuildPlan(state, currentWeekAnchor, recalibrated: profileChanged);
    }

    private static WeeklyProgressionPlan BuildPlan(WeeklyProgressionState state, DateOnly weekAnchor, bool recalibrated) =>
        new(
            WeekAnchorDate: weekAnchor,
            MesocycleWeekIndex: state.MesocycleWeekIndex,
            Decision: state.LastDecision,
            Axis: state.LastAxis,
            VolumeSetsDelta: state.VolumeSetsDelta,
            RpeDelta: state.RpeDelta,
            RestSecondsDelta: state.RestSecondsDelta,
            DeloadWeek: state.DeloadDue,
            RecalibratedFromProfileChange: recalibrated);

    // US-034/US-241 RN-006: hash simples e determinístico dos campos que disparam recalibração.
    private static string ComputeProfileHash(UserProfile profile)
    {
        var raw = string.Join('|',
            profile.WeightKg, profile.AvailableMinutesPerWorkout, profile.Goal, profile.ExperienceLevel,
            string.Join(',', profile.EquipmentAvailable.OrderBy(e => e, StringComparer.Ordinal)));
        var bytes = SHA256.HashData(Encoding.UTF8.GetBytes(raw));
        return Convert.ToHexString(bytes);
    }

    private static DateOnly WeekAnchorFor(DateOnly date)
    {
        var diff = ((int)date.DayOfWeek + 6) % 7; // segunda-feira = âncora da semana
        return date.AddDays(-diff);
    }
}

/// US-241 §18: plano consumido por US-153 (prescrição) e US-240 (blueprint).
public sealed record WeeklyProgressionPlan(
    DateOnly WeekAnchorDate,
    int MesocycleWeekIndex,
    string Decision,
    string? Axis,
    int VolumeSetsDelta,
    int RpeDelta,
    int RestSecondsDelta,
    bool DeloadWeek,
    bool RecalibratedFromProfileChange);
```

- [ ] **Step 1:** Escrever os testes com mocks de `IWeeklyProgressionStateRepository`/`IQuestLogRepository`/`IDateTimeService` (Moq, mesmo padrão de `GetResolvedProgramDayQueryHandlerTests`):
```csharp
[Fact]
public async Task ReviewAsync_CreatesInitialStateOnFirstEvaluation()
{
    _stateRepository.Setup(r => r.GetByUserIdAsync(_userId, It.IsAny<CancellationToken>()))
        .ReturnsAsync((WeeklyProgressionState?)null);
    _questLogRepository.Setup(r => r.GetCompletedSinceAsync(_userId, It.IsAny<DateTime>(), It.IsAny<CancellationToken>()))
        .ReturnsAsync([]);

    var plan = await CreateReviewer().ReviewAsync(_userId, _profile, progression: null, CancellationToken.None);

    plan.Decision.Should().Be("hold"); // sem dados -> mantém
    _stateRepository.Verify(r => r.AddAsync(It.IsAny<WeeklyProgressionState>(), It.IsAny<CancellationToken>()), Times.Once);
}

[Fact]
public async Task ReviewAsync_ReturnsSamePlanWithoutRedecidingWithinSameWeekAndNoProfileChange()
{
    var existing = WeeklyProgressionState.CreateInitial(_userId, _currentWeekAnchor, ComputeExpectedHash(_profile), DateTime.UtcNow);
    _stateRepository.Setup(r => r.GetByUserIdAsync(_userId, It.IsAny<CancellationToken>())).ReturnsAsync(existing);

    var plan = await CreateReviewer().ReviewAsync(_userId, _profile, progression: null, CancellationToken.None);

    plan.RecalibratedFromProfileChange.Should().BeFalse();
    _questLogRepository.Verify(r => r.GetCompletedSinceAsync(
        It.IsAny<Guid>(), It.IsAny<DateTime>(), It.IsAny<CancellationToken>()), Times.Never);
}

[Fact] // CA-004
public async Task ReviewAsync_RecalibratesWhenProfileHashChanges()
{
    var existing = WeeklyProgressionState.CreateInitial(_userId, _currentWeekAnchor, "old-hash", DateTime.UtcNow);
    _stateRepository.Setup(r => r.GetByUserIdAsync(_userId, It.IsAny<CancellationToken>())).ReturnsAsync(existing);
    _questLogRepository.Setup(r => r.GetCompletedSinceAsync(_userId, It.IsAny<DateTime>(), It.IsAny<CancellationToken>())).ReturnsAsync([]);

    var plan = await CreateReviewer().ReviewAsync(_userId, _profile, progression: null, CancellationToken.None);

    plan.RecalibratedFromProfileChange.Should().BeTrue();
}
```
(Notas de implementação do teste: `_profile` é um `UserProfile` construído via reflection/factory já usado em outros testes deste projeto — verificar `UserProfileTests.cs` ou fixtures existentes para o padrão de criação; `ComputeExpectedHash` deve replicar exatamente a lógica privada — como é privada, o mais simples é rodar o reviewer 1x pra "descobrir" o hash gerado e reusar, ou tornar `ComputeProfileHash` `internal` com `[assembly: InternalsVisibleTo("Awaken.UnitTests")]` se esse atributo já existir no projeto — checar `Awaken.Application.csproj`/`AssemblyInfo` antes de decidir.)
- [ ] **Step 2:** Rodar, confirmar falha.
- [ ] **Step 3:** Implementar `WeeklyProgressionReviewer.cs`.
- [ ] **Step 4:** Rodar os testes — devem passar. Ajustar detalhes de mock conforme a fixture real de `UserProfile` do projeto.

---

## Task 10 — Aplicar o `WeeklyProgressionPlan` na geração (`WorkoutGeneratorService` + `Quest`)

**Files:**
- Modify: `backend/src/Awaken.Infrastructure/Services/WorkoutGeneratorService.cs`
- Modify: `backend/src/Awaken.Application/Common/Interfaces/IWorkoutGeneratorService.cs`
- Modify: `backend/src/Awaken.Domain/Entities/Quests/Quest.cs`
- Modify: `backend/src/Awaken.Application/Quests/Commands/GenerateDailyQuest/GenerateDailyQuestCommandHandler.cs`
- Modify: `backend/src/Awaken.Infrastructure/DependencyInjection.cs`

### 10.1 — `WorkoutGeneratorService` recebe `WeeklyProgressionReviewer` via DI

Construtor ganha `Awaken.Application.Progression.Common.WeeklyProgressionReviewer weeklyProgressionReviewer`. Como `WorkoutGeneratorService` já recebe `userId`/`fitnessProfileJson` mas não o `UserProfile`/`HunterProgression` originais (só o JSON serializado), a chamada ao reviewer precisa do perfil/progressão reais — **não** re-parseá-los do JSON (perda de tipo). Ajustar a assinatura pública:

```csharp
Task<WorkoutGenerationResult> GenerateWorkoutJsonAsync(
    Guid userId,
    string language,
    string fitnessProfileJson,
    Awaken.Domain.Entities.Onboarding.UserProfile? profile = null,
    Awaken.Domain.Entities.Progression.HunterProgression? progression = null,
    CancellationToken cancellationToken = default);
```
(parâmetros trailing opcionais — não quebra os 2 chamadores existentes que não os passam; `SelectSubstituteExerciseAsync` não precisa mudar). Em `GenerateDailyQuestCommandHandler.cs`, o `workoutGeneratorService.GenerateWorkoutJsonAsync(...)` já tem `profile`/`progression` carregados — passar os dois.

Dentro de `GenerateWorkoutJsonAsync`, logo após `var profile = ParseProfile(fitnessProfileJson);`, adicionar:
```csharp
        var weeklyPlan = userProfile is not null
            ? await weeklyProgressionReviewer.ReviewAsync(userId, userProfile, hunterProgression, cancellationToken)
            : null;
```
(Renomear o parâmetro do método `profile`/`progression` recebidos para `userProfile`/`hunterProgression` para não colidir com a variável local `profile` já existente, que é o `ParsedFitnessProfile`.)

### 10.2 — Aplicar o plano na prescrição

Logo depois de `var prescription = ExercisePrescriptionEngine.Prescribe(...)`, adicionar:
```csharp
        prescription = ApplyWeeklyProgressionPlan(prescription, weeklyPlan);
```
E o método privado:
```csharp
    /// US-241: aplica no máximo 1 eixo de ajuste (RN-003) sobre a prescrição base
    /// (US-153). O teto de séries do blueprint (US-239/US-240, `volumeBudgetByGroup`)
    /// continua soberano — aplicado depois, em `SetsFor` (Math.Min já existente).
    private static ExercisePrescription ApplyWeeklyProgressionPlan(
        ExercisePrescription prescription, WeeklyProgressionPlan? plan)
    {
        if (plan is null) return prescription;

        var sets = Math.Max(1, prescription.Sets + plan.VolumeSetsDelta);
        var restSeconds = Math.Max(20, prescription.RestSeconds + plan.RestSecondsDelta);
        var repsMin = plan.Axis == "reps" && plan.Decision == "progress" ? prescription.RepsMin + 1 : prescription.RepsMin;
        var repsMax = plan.Axis == "reps" && plan.Decision == "progress" && prescription.RepsMax is int max ? max + 1 : prescription.RepsMax;

        if (plan.DeloadWeek)
        {
            sets = Math.Max(1, (int)Math.Round(prescription.Sets * 0.5, MidpointRounding.AwayFromZero));
            restSeconds = prescription.RestSeconds;
        }

        return prescription with { Sets = sets, RepsMin = repsMin, RepsMax = repsMax, RestSeconds = restSeconds };
    }
```
(`ExercisePrescription` precisa ser `record` com suporte a `with` — já é um `record`, confirmado na leitura do arquivo original.)

### 10.3 — Persistir o plano na `Quest` + expor pro Flutter

Em `Quest.cs`, adicionar:
```csharp
    // US-241: snapshot do WeeklyProgressionPlan usado nesta geração, para auditoria e avisos (US-050).
    public string? WeeklyProgressionPlanJson { get; private set; }

    public void AssignWeeklyProgressionPlan(string weeklyProgressionPlanJson, DateTime utcNow)
    {
        WeeklyProgressionPlanJson = weeklyProgressionPlanJson;
        UpdatedAtUtc = utcNow;
    }
```
Em `WorkoutGenerationResult`, adicionar (trailing, default `null`): `string? WeeklyProgressionPlanJson = null`. Em `WorkoutGeneratorService`, serializar `weeklyPlan` com `JsonSerializer.Serialize(weeklyPlan, CamelCaseJsonOptions)` quando não nulo, e incluir no retorno + no `catalogWorkout` anônimo (`deloadWeek = weeklyPlan?.DeloadWeek ?? false`, `progressionDecision = weeklyPlan?.Decision`, `recalibratedFromProfileChange = weeklyPlan?.RecalibratedFromProfileChange ?? false`).

Em `GenerateDailyQuestCommandHandler.cs`, junto ao bloco da Task 3.4, adicionar:
```csharp
        if (workoutResult.WeeklyProgressionPlanJson is not null)
            quest.AssignWeeklyProgressionPlan(workoutResult.WeeklyProgressionPlanJson, utcNow);
```

### 10.4 — DI

Em `DependencyInjection.cs`, registrar `services.AddScoped<WeeklyProgressionReviewer>();` e `services.AddScoped<IWeeklyProgressionStateRepository, WeeklyProgressionStateRepository>();` no mesmo bloco onde `DailyWorkoutBlueprintBuilder`/`IMuscleRecoveryStateRepository` já são registrados.

- [ ] **Step 1:** Aplicar 10.1-10.4.
- [ ] **Step 2:** Rodar `dotnet build`. Corrigir os testes de `WorkoutGeneratorServiceTests` que constroem o serviço manualmente (`new WorkoutGeneratorService(...)`) — vão precisar de um mock a mais (`Mock<WeeklyProgressionReviewer>` **não é mockável diretamente por ser classe concreta sem interface** — extrair uma interface `IWeeklyProgressionReviewer` se os testes exigirem mock, seguindo o mesmo precedente de `DailyWorkoutBlueprintBuilder`, que **também é classe concreta sem interface** e é testado via `WorkoutGeneratorServiceTests` construindo o builder de verdade com repositórios mockados (não mockando o builder em si). Replicar esse padrão: os testes de `WorkoutGeneratorServiceTests` devem construir um `WeeklyProgressionReviewer` real com `IWeeklyProgressionStateRepository`/`IQuestLogRepository`/`IDateTimeService` mockados, não uma interface nova.
- [ ] **Step 3:** Rodar `dotnet test backend/tests/Awaken.UnitTests --filter FullyQualifiedName~WorkoutGeneratorServiceTests`, corrigir fixtures.
- [ ] **Step 4:** Rodar a suíte completa, corrigir qualquer regressão em `GenerateDailyQuestCommandHandlerTests`/testes de integração.

---

## Task 11 — Migration `WeeklyProgressionState` + coluna `WeeklyProgressionPlanJson` em `Quest` + endpoint `GET /api/quests/weekly-progression`

**Files:**
- Modify: `backend/src/Awaken.Infrastructure/Persistence/Configurations/QuestConfiguration.cs` (+`WeeklyProgressionPlanJson`, sem limite de tamanho — mesmo padrão de `DailyWorkoutBlueprintJson`)
- Create: `backend/src/Awaken.Application/Quests/Queries/GetWeeklyProgression/GetWeeklyProgressionQuery.cs`
- Create: `backend/src/Awaken.Application/Quests/Queries/GetWeeklyProgression/GetWeeklyProgressionQueryHandler.cs`
- Create: `backend/src/Awaken.Contracts/Quests/WeeklyProgressionResponse.cs`
- Modify: `backend/src/Awaken.Api/Controllers/V1/QuestsController.cs`
- Test: `backend/tests/Awaken.UnitTests/Quests/GetWeeklyProgressionQueryHandlerTests.cs`

```csharp
// GetWeeklyProgressionQuery.cs
using Awaken.Contracts.Quests;
using MediatR;

namespace Awaken.Application.Quests.Queries.GetWeeklyProgression;

public record GetWeeklyProgressionQuery : IRequest<WeeklyProgressionResponse>;
```
```csharp
// GetWeeklyProgressionQueryHandler.cs — mesmo template de GetResolvedProgramDayQueryHandler.
using Awaken.Application.Common.Exceptions;
using Awaken.Application.Common.Interfaces;
using Awaken.Application.Progression.Common;
using Awaken.Contracts.Quests;
using Awaken.Domain.Repositories;
using MediatR;

namespace Awaken.Application.Quests.Queries.GetWeeklyProgression;

public class GetWeeklyProgressionQueryHandler(
    IUserProfileRepository userProfileRepository,
    IHunterProgressionRepository hunterProgressionRepository,
    WeeklyProgressionReviewer reviewer,
    ICurrentUserService currentUserService) : IRequestHandler<GetWeeklyProgressionQuery, WeeklyProgressionResponse>
{
    public async Task<WeeklyProgressionResponse> Handle(GetWeeklyProgressionQuery request, CancellationToken cancellationToken)
    {
        var userId = currentUserService.UserId;
        var profile = await userProfileRepository.GetByUserIdAsync(userId, cancellationToken)
            ?? throw new NotFoundException("UserProfile", userId);
        var progression = await hunterProgressionRepository.GetByUserIdAsync(userId, cancellationToken);

        var plan = await reviewer.ReviewAsync(userId, profile, progression, cancellationToken);

        return new WeeklyProgressionResponse(
            plan.WeekAnchorDate.ToString("yyyy-MM-dd"),
            plan.MesocycleWeekIndex,
            plan.DeloadWeek,
            plan.RecalibratedFromProfileChange,
            progression?.Rank ?? "E",
            plan.Decision,
            plan.Axis,
            plan.VolumeSetsDelta,
            plan.RpeDelta,
            plan.RestSecondsDelta);
    }
}
```
```csharp
// WeeklyProgressionResponse.cs
namespace Awaken.Contracts.Quests;

public record WeeklyProgressionResponse(
    string WeekAnchorDate,
    int MesocycleWeekIndex,
    bool DeloadWeek,
    bool RecalibratedFromProfileChange,
    string Rank,
    string Decision,
    string? Axis,
    int VolumeSetsDelta,
    int RpeDelta,
    int RestSecondsDelta);
```
Em `QuestsController.cs`, adicionar (mesmo padrão do endpoint `resolve-day` já existente):
```csharp
    [HttpGet("weekly-progression")]
    public async Task<ActionResult<WeeklyProgressionResponse>> GetWeeklyProgression(CancellationToken cancellationToken) =>
        Ok(await mediator.Send(new GetWeeklyProgressionQuery(), cancellationToken));
```

- [ ] **Step 1:** Escrever teste do handler (mesmo padrão de `GetResolvedProgramDayQueryHandlerTests.cs`):
```csharp
[Fact]
public async Task Handle_ReturnsWeeklyProgressionPlanForCurrentUser()
{
    _userProfileRepository.Setup(r => r.GetByUserIdAsync(_userId, It.IsAny<CancellationToken>())).ReturnsAsync(_profile);
    _hunterProgressionRepository.Setup(r => r.GetByUserIdAsync(_userId, It.IsAny<CancellationToken>())).ReturnsAsync((HunterProgression?)null);

    var response = await CreateHandler().Handle(new GetWeeklyProgressionQuery(), CancellationToken.None);

    response.Rank.Should().Be("E");
    response.Decision.Should().NotBeNullOrEmpty();
}
```
(`WeeklyProgressionReviewer` é construído de verdade com repositórios mockados, mesmo padrão da Task 9/10.)
- [ ] **Step 2:** Rodar, confirmar falha.
- [ ] **Step 3:** Implementar query/handler/response/controller + config da coluna.
- [ ] **Step 4:** Gerar migration:
```bash
dotnet ef migrations add AddWeeklyProgressionState -p backend/src/Awaken.Infrastructure -s backend/src/Awaken.Api
```
- [ ] **Step 5:** Rodar `dotnet build` + `dotnet test backend/tests/Awaken.UnitTests` completo. Corrigir tudo que quebrar.
- [ ] **Step 6:** Criar teste de integração em `backend/tests/Awaken.IntegrationTests/` (novo arquivo `WeeklyProgressionEndpointTests.cs`, mesmo padrão de `TrainingProgramsEndpointTests.cs`): `GET /api/v1/quests/weekly-progression` autenticado retorna 200 com corpo válido; sem autenticação retorna 401.

---

## Task 12 — Flutter: banners de tempo/progressão + diálogo de sentimento (Onda 3, subagente em paralelo)

> Disparar esta task como subagente assim que as Tasks 3 e 11 estiverem com o `dotnet build` passando (contrato de API fechado: nomes de campo `estimatedDurationSeconds`, `timeAdjustmentApplied`, `isMicroQuest` no `WorkoutDto`; `deloadWeek`/`progressionDecision`/`recalibratedFromProfileChange` no workout JSON; `PerceivedFeeling` no `CompleteQuestCommand`).

**Files:**
- Modify: `apps/mobile/lib/features/quests/domain/entities/quest_preview.dart`
- Modify: `apps/mobile/lib/features/quests/data/dtos/quest_response_dto.dart`
- Create: `apps/mobile/lib/features/quests/presentation/widgets/pre_quest_time_budget_banner.dart`
- Create: `apps/mobile/lib/features/quests/presentation/widgets/pre_quest_progression_banner.dart`
- Modify: `apps/mobile/lib/features/quests/presentation/pages/pre_quest_page.dart`
- Create: `apps/mobile/lib/features/quests/presentation/widgets/perceived_feeling_dialog.dart`
- Modify: `apps/mobile/lib/features/quests/presentation/pages/quest_execution_page.dart`
- Modify: `apps/mobile/lib/features/quests/presentation/providers/quest_execution_controller.dart`
- Modify: `apps/mobile/lib/features/quests/data/repositories/quests_repository_impl.dart`
- Modify: `apps/mobile/lib/l10n/app_pt.arb` / `app_en.arb` / `app_es.arb` / `app_fr.arb`
- Test: `apps/mobile/test/features/quests/presentation/widgets/pre_quest_time_budget_banner_test.dart` (novo)
- Test: `apps/mobile/test/features/quests/presentation/widgets/pre_quest_progression_banner_test.dart` (novo)
- Test: `apps/mobile/test/features/quests/presentation/widgets/perceived_feeling_dialog_test.dart` (novo)
- Test: `apps/mobile/test/e2e/pre_quest_flow_test.dart` (ajustar se quebrar)

### 12.1 — `QuestPreview`/DTO: novos campos

Em `quest_preview.dart`, adicionar ao `QuestPreview`:
```dart
    this.isMicroQuest = false,
    this.timeAdjustmentApplied,
    this.deloadWeek = false,
    this.progressionDecision,
    this.recalibratedFromProfileChange = false,
```
com os respectivos campos `final bool isMicroQuest;`, `final String? timeAdjustmentApplied;`, `final bool deloadWeek;`, `final String? progressionDecision;`, `final bool recalibratedFromProfileChange;`.

Em `quest_response_dto.dart`, localizar onde `hasMuscleGroupInRecovery`/`resolvedDayKey` são parseados do JSON do `workout` e adicionar o parse dos novos campos (mesmo padrão `json['campo'] as bool? ?? false` / `as String?`).

### 12.2 — `PreQuestTimeBudgetBanner` (mesmo padrão de `PreQuestRecoveryBanner`)

```dart
import 'package:awaken/design_system/tokens/colors.dart';
import 'package:awaken/design_system/tokens/spacing.dart';
import 'package:awaken/design_system/tokens/typography.dart';
import 'package:awaken/l10n/app_localizations.dart';
import 'package:flutter/material.dart';

/// US-242: aviso quando o treino foi ajustado ao tempo disponível (micro quest
/// ou corte de volume), no mesmo padrão visual de [PreQuestRecoveryBanner].
class PreQuestTimeBudgetBanner extends StatelessWidget {
  const PreQuestTimeBudgetBanner({super.key, required this.isMicroQuest});

  final bool isMicroQuest;

  @override
  Widget build(BuildContext context) {
    final l10n = AppLocalizations.of(context);

    return Container(
      key: const Key('pre-quest-time-budget-banner'),
      width: double.infinity,
      margin: const EdgeInsets.only(bottom: AwakenSpacing.md),
      padding: const EdgeInsets.symmetric(
        horizontal: AwakenSpacing.md,
        vertical: AwakenSpacing.smd,
      ),
      decoration: BoxDecoration(
        color: AwakenColors.cardElevated,
        border: Border.all(color: AwakenColors.info.withValues(alpha: 0.4)),
        borderRadius: BorderRadius.circular(AwakenSpacing.cardRadius),
      ),
      child: Row(
        children: [
          const Icon(Icons.timer_outlined, size: 18, color: AwakenColors.info),
          const SizedBox(width: AwakenSpacing.sm),
          Expanded(
            child: Text(
              isMicroQuest
                  ? l10n.preQuestMicroQuestBanner
                  : l10n.preQuestTimeAdjustedBanner,
              style: AwakenTypography.bodyMedium
                  .copyWith(color: AwakenColors.textSecondary),
            ),
          ),
        ],
      ),
    );
  }
}
```
(Conferir se `AwakenColors.info` existe no design system — se não, usar `AwakenColors.accent`/token equivalente já usado em outro banner informativo do projeto.)

### 12.3 — `PreQuestProgressionBanner`

Mesmo padrão, com 2 variantes de texto (`l10n.preQuestProgressionHarderBanner` quando `progressionDecision == 'progress'`, `l10n.preQuestProgressionDeloadBanner` quando `deloadWeek == true` — deload tem prioridade de exibição sobre "mais desafiador" se ambos por algum motivo fossem true). Ícone `Icons.trending_up_rounded` (progress) / `Icons.self_improvement_rounded` (deload, reaproveita o mesmo da recovery banner mas cor `AwakenColors.warning`).

### 12.4 — `pre_quest_page.dart`: renderizar os 2 banners novos

Localizar onde `PreQuestRecoveryBanner` já é condicionalmente renderizado (`if (preview.hasMuscleGroupInRecovery) const PreQuestRecoveryBanner()`), e adicionar logo abaixo:
```dart
if (preview.isMicroQuest || preview.timeAdjustmentApplied != null)
  PreQuestTimeBudgetBanner(isMicroQuest: preview.isMicroQuest),
if (preview.deloadWeek || preview.progressionDecision == 'progress')
  PreQuestProgressionBanner(
    deloadWeek: preview.deloadWeek,
    progressing: preview.progressionDecision == 'progress',
  ),
```

### 12.5 — `PerceivedFeelingDialog` (pergunta "como você se sentiu?")

```dart
import 'package:awaken/design_system/components/awaken_button.dart';
import 'package:awaken/design_system/components/awaken_panel.dart';
import 'package:awaken/design_system/tokens/spacing.dart';
import 'package:awaken/design_system/tokens/typography.dart';
import 'package:awaken/l10n/app_localizations.dart';
import 'package:flutter/material.dart';

/// US-241: pergunta simples de 3 opções ao concluir a quest — a única captura
/// real de desempenho usada para a autorregulação da progressão semanal.
/// Retorna 'too_easy' | 'just_right' | 'too_hard', ou null se descartado.
Future<String?> showPerceivedFeelingDialog(BuildContext context) {
  final l10n = AppLocalizations.of(context);

  return showDialog<String>(
    context: context,
    barrierDismissible: true,
    builder: (context) => AlertDialog(
      backgroundColor: Colors.transparent,
      contentPadding: EdgeInsets.zero,
      content: AwakenPanel(
        padding: const EdgeInsets.all(AwakenSpacing.lg),
        child: Column(
          mainAxisSize: MainAxisSize.min,
          crossAxisAlignment: CrossAxisAlignment.stretch,
          children: [
            Text(
              l10n.perceivedFeelingDialogTitle,
              style: AwakenTypography.titleMedium,
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: AwakenSpacing.md),
            AwakenButton(
              key: const Key('perceived-feeling-too-easy'),
              label: l10n.perceivedFeelingTooEasy,
              onPressed: () => Navigator.of(context).pop('too_easy'),
            ),
            const SizedBox(height: AwakenSpacing.sm),
            AwakenButton(
              key: const Key('perceived-feeling-just-right'),
              label: l10n.perceivedFeelingJustRight,
              onPressed: () => Navigator.of(context).pop('just_right'),
            ),
            const SizedBox(height: AwakenSpacing.sm),
            AwakenButton(
              key: const Key('perceived-feeling-too-hard'),
              label: l10n.perceivedFeelingTooHard,
              onPressed: () => Navigator.of(context).pop('too_hard'),
            ),
          ],
        ),
      ),
    ),
  );
}
```
(Antes de escrever, abrir `AwakenButton`/`AwakenPanel` pra confirmar os parâmetros exatos do construtor — seguir o mesmo padrão usado em `showQuestConfirmDialog`, que fica em `widgets/quest_action_bar.dart` ou arquivo irmão; reaproveitar a mesma estrutura de diálogo em vez de inventar uma nova.)

### 12.6 — Hook em `quest_execution_page.dart._handleComplete`

```dart
  Future<void> _handleComplete() async {
    final feeling = await showPerceivedFeelingDialog(context);
    if (!mounted) return;
    try {
      await ref
          .read(questExecutionControllerProvider.notifier)
          .complete(widget.questId, perceivedFeeling: feeling);
      if (!mounted) return;
      context.go(
        AppRoutes.questReward.replaceFirst(':questId', widget.questId),
      );
    } catch (_) {
      if (!mounted) return;
      final l10n = AppLocalizations.of(context);
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(content: Text(l10n.questExecutionCompleteErrorMessage)),
      );
    }
  }
```
Em `quest_execution_controller.dart`, o método `complete(String questId)` ganha parâmetro nomeado opcional `String? perceivedFeeling`, propagado para `repository.completeQuest(questId, perceivedFeeling: perceivedFeeling)`. Em `quests_repository_impl.dart`, o método `completeQuest` ganha o mesmo parâmetro opcional e inclui `if (perceivedFeeling != null) 'perceivedFeeling': perceivedFeeling` no corpo da requisição POST.

### 12.7 — Chaves i18n (adicionar nas 4 ARBs — `app_pt.arb` é a fonte, os outros seguem a mesma chave)

```json
"preQuestMicroQuestBanner": "Treino ajustado ao seu tempo — micro quest",
"preQuestTimeAdjustedBanner": "Treino ajustado ao seu tempo disponível",
"preQuestProgressionHarderBanner": "Treino mais desafiador esta semana",
"preQuestProgressionDeloadBanner": "Semana de deload — foco na recuperação",
"perceivedFeelingDialogTitle": "Como foi esse treino?",
"perceivedFeelingTooEasy": "Fácil demais",
"perceivedFeelingJustRight": "Na medida certa",
"perceivedFeelingTooHard": "Muito difícil"
```
Traduzir para `en`/`es`/`fr` com o mesmo tom direto já usado nas outras chaves desses arquivos (conferir 2-3 chaves vizinhas em cada idioma pra manter o registro).

- [ ] **Step 1:** Aplicar 12.1-12.7.
- [ ] **Step 2:** Rodar `flutter gen-l10n` (CLAUDE.md: obrigatório após qualquer mudança de ARB).
- [ ] **Step 3:** Escrever os 3 testes de widget novos (estrutura mínima: `testWidgets` que faz `pumpWidget` do banner/dialog isolado e verifica o texto/chave/botões esperados, mesmo padrão de outros testes em `apps/mobile/test/features/quests/presentation/widgets/`).
- [ ] **Step 4:** Rodar `flutter analyze` — 0 issues.
- [ ] **Step 5:** Rodar `flutter test` completo. Corrigir `pre_quest_flow_test.dart`/`training_programs_flow_test.dart` (e2e) se algum fixture de resposta mockada precisar dos novos campos com valor default.

---

## Task 13 — Verificação final (eu, depois de todas as ondas)

- [ ] **Step 1:** `dotnet build` limpo (0 erros, 0 warnings novos).
- [ ] **Step 2:** `dotnet test backend/tests/Awaken.UnitTests` completo — 0 falhas. Anotar o número final de testes (baseline era 1551 + os ~35 novos desta plan).
- [ ] **Step 3:** `dotnet test backend/tests/Awaken.ArchitectureTests` — deve continuar só com o `Test1()` vazio (fora de escopo desta plan adicionar regras novas).
- [ ] **Step 4:** `dotnet test backend/tests/Awaken.IntegrationTests` — rodar uma vez, de forma síncrona. Se houver falhas fora de EPIC-005 (Notification/Shop/BattleLog, já flagradas como pré-existentes numa auditoria anterior), confirmar que continuam sendo as mesmas e não aumentaram.
- [ ] **Step 5:** `dotnet ef migrations list -p backend/src/Awaken.Infrastructure -s backend/src/Awaken.Api` — confirmar as 4 migrations novas na ordem certa (`AddTimeBudgetFieldsToQuests`, `AddPerceivedFeelingToQuestLogs`, `AddWeeklyProgressionState` + a coluna `WeeklyProgressionPlanJson` — pode ir junto da migration de `WeeklyProgressionState` em vez de uma 4ª migration separada, pra reduzir o número de migrations).
- [ ] **Step 6:** `dotnet ef database update -p backend/src/Awaken.Infrastructure -s backend/src/Awaken.Api` (requer `docker-compose up -d` rodando) — confirmar aplicação sem conflito.
- [ ] **Step 7:** `flutter analyze` — 0 issues. `flutter test` completo — 0 falhas.
- [ ] **Step 8:** Relatório final: o que foi implementado, número real de testes por suíte, gaps conscientes documentados (Agilidade/Foco sem alavanca numérica; heurísticas de tempo não unificadas; grafo de variante não trocado automaticamente).
