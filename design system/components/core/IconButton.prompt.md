Botão só-ícone. `label` é obrigatório (acessibilidade) e `badge` mostra contagem de alertas.

```jsx
<IconButton icon="notifications" label="Alertas" badge={3} />
<IconButton icon="close" label="Fechar" size="lg" variant="outline" />
```
