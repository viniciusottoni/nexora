Campo de texto. `numeric` alinha à direita em mono tabular — obrigatório em dinheiro, peso e quantidade.

```jsx
<Input icon="search" placeholder="Buscar produto" />
<Input numeric prefix="R$" defaultValue="64,90" />
```
