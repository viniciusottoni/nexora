Teclado numérico de 12 teclas — login por PIN e comandos do KDS. Teclas de 64px.

```jsx
<NumericKeypad value={pin} onChange={setPin} onSubmit={entrar} length={4} showDots />
```

Use `dark` dentro de `[data-surface="kds"]`.
